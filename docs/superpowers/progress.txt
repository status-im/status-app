=== 2026-07-03 — Issue 0001: wrapper absorption + auto-link — DONE (pass: true) ===

Picked up: docs/superpowers/issues/0001-statusgo-wrapper-absorption-autolink.md
(PRD: docs/superpowers/prds/2026-07-03-statusgo-nimble-package-prd.md)

What was built (all pending working-tree changes, nothing committed, matching
the branch convention):
- vendor/status-go/status_go.nim + status_go/impl.nim: wrapper absorbed
  byte-identical from nim-status-go 7f79f183 (the revision status-desktop
  pins). impl.nim gained the auto-link block: explicit archive paths
  (build/bin/libstatus.a + libsds.a — explicit, not -l, so a coexisting
  libstatus.dylib can't be silently preferred) + per-OS Go-runtime syslibs
  (macOS: CoreFoundation/Security/IOKit/-lresolv), gated by
  -d:statusGoNoAutoLink.
- vendor/status-go/statusgo.nims: new `libstatus` task — builds localized
  static libsds via nim-sds, copies it to build/bin, delegates to
  `make statusgo-library` (external NIM_SDS_LIB_DIR/INC_DIR/LIBSDS seam).
- vendor/status-go/statusgo.nimble: comment updated (wrapper travels with the
  package); manifest still fully declarative.
- vendor/nim-sds/sds.nimble (4th local patch, upstream-bound): libsdsStaticMac
  now uses the iOS localization pipeline (compileOnly → clang
  -fvisibility=hidden -fno-common → ld -r -exported_symbol '_Sds*' → ar);
  archive exports exactly the 9 _Sds* symbols.
- vendor/status-go/AGENTS.md: new — packaging patterns (declarative manifest,
  explicit-archive auto-link, static-Nim-lib localization, outside-consumer
  proof harness).

Verification (macOS arm64):
- Outside consumer (scratch dir, own git repo): requires "statusgo" via
  nimble develop --add + nimble setup -l → resolved statusgo + sds 884ce6f0 +
  ffi fb25f069; solve ~10 min (no lock, known cost), manifest eval instant.
- nim c -r consumer.nim → auto-linked, hashMessage + validateMnemonic returned
  correct results (CONSUMER_OK); otool -L: no libstatus/libsds dylib deps.
- nim c -d:statusGoNoAutoLink → link FAILS (undefined _HashMessage) — flags
  proven to come from the wrapper.
- Desktop unaffected: nim libsds statusgo.nims (shared flavor) re-verified;
  no status-desktop source/makefile touched this session.

Notes for next iterations:
- Issue 0002 (installable status-backend) is next; the static auto-link layout
  was chosen partly so the installed bin survives nimble's staging→store move.
- Consumer-proof harness documented in vendor/status-go/AGENTS.md — reuse it.
- Coexistence note: build/bin now holds both static (auto-link) and shared
  (desktop) flavors; explicit archive paths keep them from interfering.
- Carry-over from pre-PRD state (unrelated to 0001, still pending): status-go
  nimble.lock generation (vcsRevision hand-fix), desktop launch + Android
  re-verification of the statusgo.nims build path, user's on-device iOS test.

=== 2026-07-03 — Issue 0002: installable status-backend — DONE (pass: true) ===

Picked up: docs/superpowers/issues/0002-statusgo-installable-status-backend.md
(PRD: docs/superpowers/prds/2026-07-03-statusgo-nimble-package-prd.md)

What was built (all pending working-tree changes in vendor/status-go, nothing
committed, matching the branch convention):
- cmd/status-backend/server/serve.go: new Run(address) — the embeddable
  equivalent of cmd/status-backend main (logging init, SIGINT/SIGTERM logout,
  Listen + RegisterMobileAPI + Serve). main.go untouched.
- tools/generate-cbindings/{const,main}.go: generator now appends a verbatim
  StatusBackendRunServer export (like Free). It can't be parser-generated:
  the server pkg imports mobile, so the export can't live in mobile/ (cycle).
- status_go/impl.nim + status_go.nim: statusBackendRunServer wrapper procs.
- status_backend.nim (+ .nim.cfg with -d:noSignalHandler): thin bin entry,
  parses --address, calls the export through `import status_go` (the same
  wrapper+auto-link path library consumers use).
- statusgo.nimble: bin=@["status_backend"]; installDirs=@["build","status_go"];
  installFiles=@["status_go.nim","statusgo.nims"]; declarative-safe
  `before build` hook — STATUS_GO_SKIP_GO_BUILD switch, go/make prereq checks
  with actionable messages, else `nim libstatus statusgo.nims`.
- statusgo.nims: install-context support — depPathFlags runs a nested
  `nimble setup` when nimble.paths is absent (outer install already populated
  the store → seconds); sdsSourceDir honors NIM_SDS_SOURCE_DIR, falls back to
  sibling checkout, then in-package .sds-src clone.
- vendor/status-go/AGENTS.md: new section with the hard-won install patterns.

Verification (macOS arm64; scratch at ~/.cache/sg0002, kept for inspection):
- Clean nimble dir: nimble install completed; $NIMBLE_DIR/bin/status_backend
  symlink on PATH; /health answered; POST /statusgo/HashMessage "hello" →
  0x50b2c43f…b37750 (matches 0001's verified call); SIGTERM → graceful logout.
  Re-proven end-to-end after the final manifest change.
- Hybrid: file:// + throwaway local commit → real store copy
  (pkgs2/statusgo-0.1.0-*) with bin + archives + wrapper sources; consumer
  compiled against the store copy alone auto-linked and ran correctly.
- Skip switch: artifacts present → stated reuse, no Go build, install
  completes; no artifacts → stated skip, no Go build, binary step cancelled
  (nimble "Pre-hook prevented further execution" — hook-false semantics).
- go stripped from PATH → clear error naming the Go toolchain + remediation.
- Quality: golangci-lint 0 issues; lint-panics clean; desktop shared libsds
  flavor re-verified after the nims changes. No GH interaction (local only).

Gotchas discovered (also in vendor/status-go/AGENTS.md):
- bin packages are binary-only: nimble installs NOTHING but bin +
  installDirs/installFiles — wrapper sources must be whitelisted or hybrid
  consumption silently breaks.
- bare --nimblePath:<store> does not resolve modules for the inner libsds
  compile; only exact --path entries (nested `nimble setup`) work.
- nimble install <local path> (and file:// of it) builds in the SOURCE dir
  and symlinks $NIMBLE_DIR/bin to it; only URL-flavored installs create a
  pkgs2 store copy.
- very long NIMBLE_DIR → nimcache C-file names overflow macOS NAME_MAX (255):
  "cannot open '….nim.c'". Keep test nimble dirs short.

Notes for next iterations:
- Issue 0003 (desktop single-graph consumption) is next; statusgo.nims now
  works with and without nimble.paths, and NIM_SDS_SOURCE_DIR lets desktop
  point at vendor/nim-sds explicitly.
- Skip-switch caveat to revisit if criterion hardens: with no prebuilt
  artifacts the install *fails with a stated reason* instead of completing
  source-only — nimble hook semantics allow no graceful bin-skip.
- Carry-overs unchanged from 0001: status-go nimble.lock generation
  (vcsRevision hand-fix), desktop launch + Android re-verification of the
  statusgo.nims build path, user's on-device iOS test, upstreaming the
  nim-sds libsdsStaticMac localization patch.

=== 2026-07-03 — Design session: sds from nimble resolution (issues 0005/0006 filed) ===

User rejected the sds source improvisations left in statusgo.nims after 0002
(NIM_SDS_SOURCE_DIR env → sibling ../nim-sds → git clone at pin): the build
must compile the nim-sds that NIMBLE resolved, nothing else. Brainstormed and
spiked; design written to
docs/superpowers/specs/2026-07-03-sds-from-nimble-resolution-design.md.

Spike results (all local — localhost git serving; nimble 0.22.3 AND master
0.99.1 built from source):
- ?subdir=<path>@#<rev> installs work (rev must come last); each subdir is
  its own name-keyed store entry → per-target packages coexist (the
  store-collision blocker dissolves via target-in-identity).
- Subdir staging contains ONLY the subdir's files — parent repo sources are
  never visible to hooks; paths=@["../.."] does not change that. A target
  subdir package must source sds via requires (build the resolved copy).
- Bin-less packages: only install hooks fire; before-install runs in
  buildtemp and its outputs ARE installed via installDirs.
- Flag-forwarded cross-install is structurally dead on both nimble versions:
  hooks never see --os/--cpu (params/env/defines probed; source confirms
  compilation flags forward only to custom tasks), and the store dir is
  name-version-checksum with no target dimension. Bin compiles DO get the
  flags. → upstream nimble ask recorded (hook flag forwarding +
  target-aware store).

Decisions:
- Issue 0005 (open, unblocked): every statusgo.nims sds task builds the
  nimble-resolved copy — develop-linked checkout in place, store copy via
  .sds-build/ scratch; sibling/clone/env override deleted; patched sds via
  nimble develop --add (wire vendor/nim-sds develop-link for the worktree).
- Issue 0006 (open, upstream-gated): nim-sds targets/<flavor> subdir
  artifact packages reusing the same engine = one-command cross-target
  provisioning; plus file the upstream nimble issue.

Operational gotchas (now in the spec): run `nimble install <url>` from a
neutral dir (inside a package dir vNext also builds THAT package — it began
building nim_status_client into the spike store); file:// URLs bypass subdir
parsing; nimble clones to ~/.nimble/pkgcache regardless of NIMBLE_DIR.

Next pickup order: 0003 (desktop single-graph) or 0005 (engine) — 0005
partially reshapes what 0003 wires, consider doing 0005 first.

Addendum (same session): 0006 gains "sds build unification" — collapse
nim-sds's per-target tasks into ONE parametric build entry point (flags in
nim config keyed by --os/--cpu/defines; tasks become aliases). Verified
limits keeping a thin driver: nimble has no library-artifact concept (bin
builds programs only); localized static flavors are a multi-command
pipeline (no flag equivalent for post-link symbol localization); hooks
can't see --os/--cpu and repeat same-version installs silently no-op (not
overwrite); env-keyed dispatch (SDS_TARGET) leaks into dependency installs
— rejected. Spec updated (design doc + issue 0006).

=== 2026-07-03 — Issue 0005: sds build from nimble resolution — ENGINE DONE,
    desktop-substitution leg BLOCKED on user decision (nimble defect) ===

Picked up: docs/superpowers/issues/0005-sds-build-from-nimble-resolution.md
(design: docs/superpowers/specs/2026-07-03-sds-from-nimble-resolution-design.md)

What was built (pending working-tree changes, nothing committed):
- vendor/status-go/statusgo.nims: full engine rewrite. One rule: every sds
  task builds the copy resolved in nimble.paths. Non-store path → in place;
  store copy → .sds-build/ scratch (wiped+recopied each run, chmod u+w,
  filtered nimble.paths materialized inside, srcDir-hoisted layouts handled).
  Deleted: NIM_SDS_SOURCE_DIR, sibling ../nim-sds, ensureSdsSource clone,
  sdsPin(). No-sds-in-resolution → actionable quit (no clone). Per user
  direction, NO nimble-develop-feature logic in statusgo.nims.
  Install-context bootstrap reworked: nimble BLOCKS nested project actions
  under buildtemp (thereIsNimbleFile guard), so the nested `nimble setup`
  now runs on a manifest copy in a system-temp scratch dir (same NIMBLE_DIR)
  and the generated nimble.paths is copied back with the root entry rewritten.
- vendor/nim-sds/sds.nimble (5th local patch, upstream-bound):
  installDirs = @["library", "sds"] — nimble's install action otherwise
  hoists srcDir contents and DROPS library/, making installed copies unable
  to build the C archive. (installFiles for root config files is ignored by
  nimble — tested and removed.)
- Desktop Makefile: NIM_SDS_SOURCE_DIR variable+export removed (NIMSDS_CHECKOUT/
  NIMSDS_LIBDIR now local; NIMSDS_LIBDIR exported for packaging scripts;
  init_app_dir.sh + bundle-flatpak.sh take NIMSDS_LIBDIR). nimble-setup rule
  wires `nimble develop --add:vendor/nim-sds` before setup (kept: correct
  intent, currently inert due to the nimble defect below). mobile/Makefile:
  same wiring; artifact copy now via NIMSDS_LIBDIR. ADR 0003 semantics
  (order-only platform-cleanup, FORCE delegation, cmp-before-copy) untouched;
  make -n graphs verified for desktop + mobile-iOS.
- vendor/status-go/.gitignore: + .sds-build. AGENTS.md: new "sds build
  engine" + "nimble 0.22.3 resolution walls" sections.

Verification (macOS arm64):
- grep-clean: no NIM_SDS_SOURCE_DIR / sibling / clone in statusgo.nims. ✓
- Desktop in-place flavors: `nim libsds` (dylib) and `nim libstatus`
  (localized static, nm = exactly 9 _Sds*) rebuilt vendor/nim-sds in place —
  verified while the engine still resolved the develop link itself (that
  logic since reverted per user; see blocker).
- Negative: crafted sds-less nimble.paths → actionable message, no clone. ✓
- Install flow: clean NIMBLE_DIR (~/.cache/sg5/nd), statusgo copy with sds
  pinned to a PATCHED snapshot (git stash-create tag sds-0005-proof served
  via loopback dumb HTTP, http://127.0.0.1:8417/#rev — file:// requires
  don't parse #rev). `nimble install file://<copy>` → sds store copy
  (library/+sds/ layout via new installDirs) → .sds-build build → localized
  archive → make statusgo-library → status_backend on PATH → /health ✓,
  HashMessage "hello" → 0x50b2c43f…b37750 (matches 0002) ✓, SIGTERM
  graceful ✓, store pristine ✓, installed libsds.a = 9 _Sds* only ✓.
- Mobile spot-check: `nim libsdsIos statusgo.nims` in the install-proof
  copy (out-of-tree, patched store sds) → resolved from the store, built in
  .sds-build/ → arm64 libsds.a, exports = 9 _Sds* only. ✓ (In-tree mobile
  runs share the desktop-substitution question below.)

BLOCKER (user decision needed) — nimble 0.22.3 cannot substitute a
develop-linked checkout for a URL-required dependency. Full matrix, all
empirical (toy consumer + nimble source reading):
  * URL#hash requires: develop ignored (special-version binding needs
    nimblemeta specialVersions, develop copies have none) → store copy wins.
  * URL#hash + nimble.lock + nimble sync (the documented workflow): same.
  * URL + version range: "Dependency <url> not found in the graph" —
    URL requires only work as #special on this nimble.
  * bare URL (verAny): develop half-used — its DEPS resolve, but the sds
    path itself is missing from nimble.paths (import sds fails).
  * name-form requires (e.g. "statusgo"): develop links DO work (the 0001
    consumer-harness precedent) but need a registry entry for non-develop
    consumers; sds is not in the nimble registry.
Consequence: with the engine strictly nimble.paths-driven, the desktop
worktree currently builds the UNPATCHED store sds → fails today (in-tree
parent-config poisoning without NIMFLAGS forwarding; static-mac archive
unlocalized → 17 duplicate-symbol link errors — both empirically shown).
Options for the user: (1) allow a small documented shim in statusgo.nims
that honors nimble.develop until nimble fixes it (was built + verified,
then reverted on request); (2) register sds in nim-lang/packages and move
to name-form requires + lock-carried pin; (3) live without local sds
substitution until the sds patch queue merges upstream and the pin bumps
(post-merge the store-copy path works even in-tree); (4) pin a patched
nimble. Upstream nimble issues to file either way: develop-vs-URL-requires
(all flavors above), install-vs-setup store-content divergence
(srcDir-hoisting drops non-srcDir dirs; same checksum dir, different
contents), buildtemp guard vs legitimate hook bootstraps, hook flag
forwarding + target-blind store (from the design spike).

Harness/cleanup notes: loopback git serving of vendor/nim-sds/.git on
:8417 may still be running (python http.server, kill freely); stray
`git daemon` on ~/.cache/sgspike is from the earlier spike session.
Proof tag sds-0005-proof (vendor/nim-sds) = stash-create snapshot of the
patched working tree (recoverable state; delete after upstreaming).
~/.cache/sg5 kept for inspection. statusgo-nimbledeps nimble.paths was
regenerated (libp2p drifted 2.1.2→2.1.3 — the known stale-lock/no-lock
drift; regenerate or pin as needed).

=== 2026-07-04 — Issue 0005 RESOLVED (pass: true) — user decision:
    manifest-level substitution ===

User rejected develop-mode workarounds and chose: "when I develop, edit the
nimble file and point it at my local repo". Verified and wired:
- statusgo.nimble interim state: `requires "file:///…/vendor/nim-sds"`
  (ABSOLUTE path; relative `file://../nim-sds` and bare-URL forms silently
  drop the dep from nimble.paths). nimble resolves the checkout with LINK
  semantics — nimble.paths points into vendor/nim-sds, edits propagate, the
  0005 engine builds it in place. GitHub #pin restored when the sds patch
  queue merges upstream (pin kept in an adjacent comment).
- New nimble 0.22.3 defect found while wiring: a file:// dependency and a
  sibling URL#hash requires (the ffi pin) SILENTLY DROP EACH OTHER from
  nimble.paths. Interim state therefore also comments out the ffi pin; ffi
  resolves via patched sds's `>= 0.1.3 & < 0.2.0` cap (currently 0.1.5).
- ffi 0.1.5 enforces signal-ownership: libsdsIos/libsdsAndroid tasks now
  pass -d:noSignalHandler like the host tasks (always correct — libsds is
  embedded everywhere; 0.1.4 just didn't enforce).
- Makefiles: the (inert) `nimble develop --add` wiring removed from desktop
  + mobile setup rules; comments describe the manifest mechanism.
- Re-verified after the flip: `nim libsds` (dylib), `nim libstatus`
  (localized static, 9 _Sds*/0 others), `nim libsdsIos` (arm64, minos 16.2,
  LC_BUILD_VERSION iOS) — all built vendor/nim-sds IN PLACE, no .sds-build;
  desktop + mobile-iOS `make -n` graphs unchanged. Issue file, PRD table
  (pass: true), AGENTS.md (file:// + dual-pin + ffi-0.1.5 gotchas) updated.

Carry-overs: restore both GitHub pins (sds + ffi) when the 5-patch sds
queue (ffi cap, iOS -fno-common, NIMFLAGS forwarding, libsdsStaticMac
localization, installDirs whitelist) lands upstream; ffi floats to 0.1.5
in the interim (certified pin was 0.1.4). nim-sds patches still
uncommitted in vendor/nim-sds (user to decide on committing, signing).
Upstream nimble issues to file (see 0003-session list + dual-pin drop).
Desktop app build+launch and Android leg re-verification remain the
standing pre-0003 carry-overs.

=== 2026-07-06 — Issue 0003: desktop single-graph consumption — DONE (pass: true) ===

Picked up: docs/superpowers/issues/0003-desktop-single-graph-consumption.md
(PRD: docs/superpowers/prds/2026-07-03-statusgo-nimble-package-prd.md)

What was built (pending working-tree changes, nothing committed):
- nim_status_client.nimble: nim-status-go wrapper pin DROPPED; statusgo
  required via ABSOLUTE file:// (interim — nimble 0.22.3 refuses to LOAD a
  develop-linked package whose manifest has file:// requires, and
  statusgo.nimble interim-carries the file:// sds pin; final name-form +
  develop shape documented in-place). json_rpc bumped 0.6.0→0.6.1 (websock
  <0.4.0 cap vs libp2p 2.x's websock >=0.4.0).
- Makefile: STATUSGO_NIMBLE_DIR (~/.cache/statusgo-nimbledeps) + its
  `nimble setup` DELETED from desktop + mobile Makefiles;
  vendor/status-go/nimble.paths is now a cmp-gated COPY of the app's
  nimble.paths. Setup stamp = nimble.paths itself, keyed on lock + all three
  manifests (app, statusgo, sds). App store moved OUT of tree:
  APP_NIMBLE_DIR ?= ~/.cache/status-desktop-nimbledeps (nimble setup builds
  dep binaries — dnsclient via libp2p — and Nim's parent-dir config walk
  poisons in-tree builds; in nested worktrees the ENCLOSING checkout's
  config.nims leaks in, unfixable from inside). run-macos gained
  NIMSDS_LIBDIR on DYLD_LIBRARY_PATH (libstatus references
  @rpath/libsds.dylib; linux targets already had it — desktop launch was the
  standing pre-0003 carry-over and this was its missing piece).
- config.nims: all app-specific switches gated on NOT compiling a dependency
  (projectPath under nimbledeps/) — converted the file to switch()-in-if
  form; -d:statusGoNoAutoLink added (single place covers desktop, mobile,
  tests); isaac /src hack now parses nimble.paths instead of scanning
  nimbledeps/pkgs2.
- vendor/nim-sds/sds.nimble (local patch queue extended): ffi requires
  range→#fb25f069 pin (the CI-certified 0.1.4; upstream released ffi 0.2.0 on
  Jul 4 whose cbor_serialization dep HARD-FAILS nimble's graph expansion for
  any consumer resolving the range). libp2p floor kept at upstream >=1.15.2
  with a comment documenting why forcing 2.1.x is impossible (see below).
- BUILDING.md: nimble section rewritten (out-of-tree store, single graph,
  NIMBLE_DIR for ad-hoc nimble commands, fixed the stale nimsds.mk bullet and
  the wrong "nimble develop --add" advice). vendor/status-go/AGENTS.md: new
  "status-desktop single-graph consumption" section + 5 new resolution walls.
- nimble.lock regenerated for the merged graph (37 pkgs; ffi #fb25f069 in,
  wrapper gone) + ONE hand-fix: nimble records websock at 0.3.0 while the
  resolution picks 0.4.0 — clean-store solveLockFileDeps hard-fails on the
  divergence (warm stores mask it). Hand-set version/vcsRevision/checksum.

Verification (macOS arm64; details in the issue file):
- Single setup materializes 41 pkgs incl. statusgo→vendor/status-go,
  sds→vendor/nim-sds (link semantics), ffi 0.1.4, libp2p 2.0.0, websock
  0.4.0, lsquic 0.5.4. grep-clean: no statusgo-nimbledeps anywhere.
- Desktop REBUILD_NIM build compiled the wrapper from vendor/status-go
  (nimcache module path proof) and `make run` launched StatusDev.app:
  status-go up (media server, 0 errors in pre_login.log), graceful shutdown.
  Full chain re-verified from a WIPED store (paths copy → libsds in-place
  rebuild → app relink → launch).
- Edit propagation: comment appended to vendor/nim-sds source → libsds
  rebuilt in place, no reinstall (probe reverted).
- Clean-env: store wipe → make nimble-deps = 3:56, download-bound, NO SAT
  solve (was ~11.5 min extra).

nimble 0.22.3 walls discovered (all empirical, documented in
vendor/status-go/AGENTS.md "resolution walls"; upstream-issue fodder):
1. file:// requires legal only top-level or from file:// packages → a
   develop-linked package whose manifest has one refuses to load.
2. URL+range requires evaluate EVERY tag's manifest; one candidate with an
   unresolvable dep (ffi 0.2.0 → cbor_serialization@0.3.0) kills the WHOLE
   solve via the reachability check. #hash pins skip enumeration.
3. Special-version pre-binding: any ENUMERATED candidate requiring pkg#hash
   pre-binds pkg globally (first-wins) — libp2p 1.15.x manifests pin
   lsquic#6ae249c5 (manifest 0.0.1), so libp2p 2.1.x (lsquic >=0.5.4) is
   UNSATISFIABLE in merged graphs; solver lands on 2.0.0. Verified failing
   counter-measures: floor raises (enumeration unfiltered), libp2p#hash from
   a file:// chain (doesn't resolve), top-level lsquic pin (specials don't
   reliably carry speSemanticVersion → can't satisfy >= ranges; a pin whose
   version must meet a floor can make the graph UNSAT while pin-less solves).
4. Version tables are nondeterministic: (url,range)-keyed pkgcache clones
   never refresh (Jul-2 clone hides Jul-3 tags) + anonymous GitHub API rate
   limit (60/h) silently degrades discovery. Purge stale
   ~/.nimble/pkgcache/<pkg>_<range> clones when a known version is missing.
5. nimble lock ↔ setup divergence (websock 0.3.0 vs 0.4.0; file:// subtree
   omitted): warm stores mask it, clean stores hard-fail → hand-fix pattern.

Notes for next iterations:
- 0004 (mobile matrix) is the next unblocked issue. Mobile Makefile already
  points at the derived nimble.paths copy; ../nimble.paths existence is
  guarded with an actionable error. -d:statusGoNoAutoLink comes from
  config.nims which mobile compiles also read. Expect the same libp2p-2.0.0
  matrix for libsds mobile flavors (spot-check iOS first).
- Store sharing: ~/.cache/status-desktop-nimbledeps is shared across
  checkouts/worktrees by design (content-addressed store).
- libsds now builds against libp2p 2.0.0 (was 2.1.3 on Jul 3-4, upstream sds
  CI certifies 1.15.2; the merged-graph SAT walls make 2.1.x unreachable on
  nimble 0.22.3). Desktop-verified; keep an eye on messaging QA.
- Carry-overs: restore GitHub pins (sds file:// → URL, app statusgo file:// →
  name-form + nimble develop) when the sds patch queue merges upstream; the
  ffi pin can then return to upstream's range IF nimble fixes expansion (wall
  2). Android re-verification + user's on-device iOS test still pending.
  Upstream nimble issues to file: walls 1-5 above + the 0005-session list.

=== 2026-07-06 — Issue 0004: mobile matrix on the new package — DONE (pass: true) ===

Picked up: docs/superpowers/issues/0004-mobile-matrix-new-package.md
(PRD: docs/superpowers/prds/2026-07-03-statusgo-nimble-package-prd.md)

What was built/verified (pending working-tree changes, nothing committed):
- iOS device app under the single graph: Status.app built + signed (team
  8B5X2M6H2Y, codesign --verify --deep --strict OK), Qt 6.11.0/ios kit (the
  6.9.2 kit in older notes is gone from this machine). libsds.a symbol
  surface: nm -gU = exactly 9 _Sds* exports, 0 leaked Nim-runtime globals —
  both in vendor/nim-sds/build and the mobile/lib copy. libsds iOS/Android
  cross-compiles needed NO changes for the 0003 libp2p-2.0.0 matrix.
- Android arm64-v8a APK (assembleDebug): all native libs arm64-v8a in the
  APK (libstatus.so, libsds.so, stub, service JNI, client). assembleRelease
  fails only at :packageRelease signing — gradle release signingConfig
  reads STATUS_APP_KEYSTORE_PATH (unset → /nonexistent). User-owned
  credential, not a flow defect; debug signing satisfies the ABI criterion.
- Sentinel matrix in one tree: darwin→ios→android→darwin, cleanup fired on
  each flip, zero stale-artifact link failures. Return desktop build
  correctly did not relink the client (shared-lib flavors).
- Compare-before-copy was DEAD ON ARRIVAL and got fixed for real: the first
  no-op iOS rebuild relinked+re-signed everything because libstatus/libsds
  bytes churned every build. Three root causes (all pre-existing, exposed by
  the criterion; fixes in the locally patched repos, upstream-bound):
  (1) tools/generate-cbindings ranged over Go maps → generated
      statusgo-lib/main.go reshuffled exports every run (~60 MB archive
      churn). Now emits in sorted key order.
  (2) Go link-time build ID differs run-to-run → -ldflags=-buildid= in
      statusgo-ios-library + statusgo-android-library.
  (3) ar header mtimes: iOS c-archive repacked with ZERO_AR_DATE=1 libtool
      -static -no_warning_for_no_symbols (status-go Makefile); nim-sds's
      ar rcs (iOS + static-mac tasks) prefixed ZERO_AR_DATE=1 (sds.nimble —
      extends the local sds patch queue).
  After: no-op rebuild leaves mobile/lib copies + client lib + Status.app
  binary untouched ("Building app" 0 times, ~2:20 wall); Go-source probe
  (comment in mobile/status.go) → fresh libstatus copied, app relinked and
  re-signed; untouched libsds copy not copied. Probe reverted.
- Criterion-5 audit: mobile recipes touch only package tasks, status-go
  PHONY targets, artifact-dir contract, version.sh. Deleted dead
  STATUS_GO_FILES (find over status-go sources at every make parse) and
  STATUS_GO_SCRIPT (dangling buildStatusGo.sh) from mobile/scripts/Common.mk.
- Docs: issue 0004 closed (verification record), PRD row updated, AGENTS.md
  gained a "Byte-reproducible library outputs" section (audit recipe: two
  runs + cmp; ar-mtime diff signature = char ~32/33 of an ar line).

Gotchas for next sessions:
- Determinism audit recipe: build twice, cmp; if only char ~32/33 differs
  it's ar header mtimes; else ar x both + diff members.
- go build always rewrites -o output (vendor lib mtimes advance every run);
  ONLY the cmp-gated copies are meaningful freshness signals downstream.
- Editing vendor/nim-sds/sds.nimble re-triggers `nimble setup` via the
  root nimble.paths stamp (manifest is a stamp dependency) — warm store,
  seconds, no solve; expected, not a hang.
- Android EnvVariables defaults: ANDROID_API=28, IOS_TARGET=17 (device+sim).
  Release APK/AAB legs need STATUS_APP_KEYSTORE_PATH + friends from the user.

Carry-overs: user decision on committing the extended nim-sds patch queue
(now also carries the ZERO_AR_DATE ar fix) + status-go changes (generator
sort, -buildid=, libtool repack — all upstream-PR material). On-device
mobile smoke (iOS + Android) still user-pending. Standing 0003 carry-overs
(restore GitHub pins when the sds queue merges, upstream nimble issues to
file) unchanged.

=== 2026-07-06 — addendum: 0004 on-device smokes (both platforms) ===
- iOS: Status.app installed + launched on iPhone 15 Pro Max via `make
  mobile-run` (Qt 6.11.0/ios, DEVICE_ID C6D39D50…); app ran ~2 min with a
  live profile session (RPC traffic), graceful shutdown. The trailing
  devicectl "Mercury error 1001" is the console tunnel dying AFTER app exit,
  not a failure.
- Android: debug APK installed + launched on SM-G990B (RFCRC0FY4ZJ); main
  process AND :statusgo service process both up, user interacted on-device.
  Note: debug package id is app.status.mobile.debug (pidof checks must use
  the .debug suffix).

=== 2026-07-06 — grilling session: new iteration (one-command + develop mode) ===
Interview-driven design (grill-with-docs + domain-modeling); all decisions
recorded in PRD 2026-07-06-one-command-and-develop-mode-prd.md, ADR 0007,
CONTEXT.md (new glossary), issues 0007-0012. Key outcomes:
- Front door = task not install (nimble install --os/--cpu structurally dead:
  hooks target-blind); dev-build scope; one task + --os/--cpu flags.
- MEASURED: root-manifest nimble task does NOT hang (wall is per-candidate
  dependency-manifest eval), but `nimble <task>` = ~46-48s warm overhead per
  invocation (--offline errors, --legacy 1:26) => driver is status.nims via
  `nim <task> status.nims` (~1s). Promote to nimble task only if a future
  nimble kills the tax.
- sds pin flips to PR#85 head alexjba/nim-sds@5c89d61 (whole 6-patch queue);
  ffi pin drops; vendor/nim-sds leaves the default flow (0007).
- Vendor end-state: full-pin, NO checkouts in default mode; status-go
  submodule removed after user pushes the branch (0010). Two flavors:
  nimble-graph (statusgo, sds, seaqt, nimqml; URL#hash + nimble.paths
  overlay) and cmake (status-keycard-qt, keycard-qt; FetchContent GIT_TAG +
  FETCHCONTENT_SOURCE_DIR redirect, MobileUI pattern). seaqt pair spike-gated.
- Develop mechanism = overlay (ADR 0007): stock nimble, clean manifests, no
  cascade; limitation (resolution reads pinned manifest) guarded by loud
  divergence check; file:// flip demoted to escape hatch; patched nimble =
  upstream-only track. 0005's pending mechanism pick is hereby RESOLVED.
- Rebuild gating: pinned => stamp-skip (stamp = store path + target triple +
  flags, nothing else); developed => ADR-0003 FORCE + cmp (scope narrowed,
  recorded in ADR 0007).
- make frozen (strangler ratchet): driver delegates to existing recipes, all
  new logic in nimscript, vendor conversions delete make rules; dropping make
  deferred to the Windows push.
- Process: every implementation blocker gets its own grill session; all work
  local (user pushes branches only when a pin needs reachability).
Sequencing: 0007 -> 0008 -> 0009 -> 0010; 0011 after 0009; 0012 last.

=== 2026-07-06 — Issue 0012: seaqt pair feasibility SPIKE — DONE (PASS on all four questions) ===

Spike only (conversion blocked by 0009 + mandatory grill). All experiments in
scratch with NIMBLE_DIR=~/.cache/seaqt-spike-nimbledeps; no repo-tree builds,
app store untouched. Record: docs/superpowers/specs/2026-07-06-seaqt-graph-spike.md.

- Q1 resolution PASS: requires "https://github.com/seaqt/nim-seaqt.git#2d95808"
  + "https://github.com/seaqt/nimqml-seaqt.git#c5e5831" resolve on nimble
  0.22.3 in 20 s from a fresh store. Hash pins skip enumeration; nimqml's
  non-declarative manifest (tasks) evaluated once without hanging; repo-name
  vs package-name mismatch a non-issue. nimqml srcDir contents HOISTED to the
  store entry root, so --path roots match today's config.nims switches
  exactly. specialVersions recorded in nimblemeta.json.
  NOTE: submodule pin 2d95808 = tip of upstream branch smo-6.4 (also tagged
  qt-6.4-seaqt-gen-…), NOT branch qt-6.4 (tip 21c8a80). nimqml pin is behind
  upstream master (d699ba8).
- Q2 store-copy PASS: shims compile via {.compile.} into the consumer's
  nimcache — proven against a chmod -R a-w store; NimQml smoke
  (QApplication + QQmlApplicationEngine + engine.load) builds in 11.4 s and
  runs (SMOKE-OK). sds-style .sds-build/ scratch engine NOT needed. Nuance:
  nimble setup rewrites store nimblemeta.json, so the store must stay
  owner-writable for nimble itself.
- Q3 compat include PASS: config.nims:108 -I<repo>/seaqt_compat is APP-owned
  (QVariantConstPointer shim for Qt>6.4); global passC reaches {.compile.}'d
  store sources. Positive + negative (fatal error without -I) proven. Only
  config.nims:31-32 path switches get deleted by the conversion.
- Q4 Qt flags PASS: seaqt discovers flags via gorge(pkg-config) — purely
  PATH/PKG_CONFIG_PATH env (qt-pkgconfig.mk wrapper), zero package-location
  dependence. Verified through the store compile; committed .pc trees exist
  for macos/ios/android_arm64_v8a/msvc2022_64.
- Bonus: real app manifest + lock + the two pins in scratch → full graph
  resolves (exit 0, 44 paths); CONTROL without pins = same graph, 42 paths —
  pins perturb nothing. Both arms resolve libp2p 1.15.3/websock 0.3.0 vs
  lock's websock 0.4.0 (pre-existing 0003 wall #5 divergence + 0007's
  in-flight sds flip; flagged for lock regeneration, not a 0012 finding).

Verdict: recommend PASS PATH (URL#hash pins, delete both submodules +
config.nims:31-32, nimble-graph vendor-table entries for 0009 overlay).
Conversion residuals: lock gains 2 entries (clean-store validation +
possible hand-fix pattern), mobile spot-check, record smo-6.4 branch in the
vendor table, nimqml pin-bump is a separate decision. Awaiting grill session
per the issue's blocker before any conversion work.

=== 2026-07-06 — Issue 0007: sds pin flip to PR #85 — DONE (pass: true) ===

Picked up: docs/superpowers/issues/0007-sds-pin-flip-pr85.md
(PRD: docs/superpowers/prds/2026-07-06-one-command-and-develop-mode-prd.md)

What was built:
- vendor/status-go/statusgo.nimble: interim file:// nim-sds requires ->
  `requires "https://github.com/alexjba/nim-sds.git#5c89d61f…"` (PR
  logos-messaging/nim-sds#85 head = the whole 6-patch queue); explicit ffi
  pin DROPPED (5c89d61 pins ffi itself, #fb25f069 = 0.1.4).
- Desktop Makefile: NIMSDS_CHECKOUT/vendor-nim-sds dirs -> NIMSDS_BUILD_ROOT
  = vendor/status-go/.sds-build (the statusgo.nims store-scratch engine's
  output); vendor/nim-sds/sds.nimble REMOVED from the setup-stamp deps.
  mobile/Makefile: same var flip. platform_pre_build_cleanup.sh also wipes
  .sds-build. vendor/status-go/Makefile: NIM_SDS_VERSION sed generalized +
  new NIM_SDS_REPO (clone-nim-sds URL now derived from the manifest pin).
- nim_status_client.nimble: uuids pin -> alexjba/uuids@5d79d279 fork (see
  blocker below); stale isaac comment replaced. nimble.lock regenerated
  (37 pkgs; uuids/isaac fork pins; websock 0.3.0 divergence left in — see
  revised lock understanding). BUILDING.md updated.

BLOCKER found + grilled (fork decision, user-approved, user said "just push"):
clean-store `nimble setup` with a lock hard-fails — PRE-EXISTING (old config
fails identically today; 0003's clean-store pass likely rode warm state).
Root cause via minimal repro + instrumented nimble: vNext validation reads
an EMPTY version from INI-format manifests on fresh clones -> uuids'
`isaac >= 0.1.3` fails -> uuids dropped -> lock-path reachability kills the
solve. Fix: forked+modernized manifests, alexjba/{uuids,isaac} pushed
(uuids@5d79d279 pins isaac@5bd05be4 by revision). Two NEW walls documented
in vendor/status-go/AGENTS.md; the old "hand-fix divergent lock entries"
lore is RETIRED (the lock never constrains this app's solves: the coverage
check can't match URL-form requires, so every setup re-solves and lock
entries are not solve constraints — verified with divergent websock).

Verification (all 5 criteria; record in the issue file):
- Wiped store + vendor/nim-sds moved aside: make nimble-deps 6:17 (solve
  included — see wall; matrix libp2p 2.0.0/websock 0.4.0/lsquic 0.5.4/
  ffi 0.1.4, sds from store pkgs2/sds-0.3.0-89a7872a), app built (libsds in
  vendor/status-go/.sds-build scratch, client links @rpath/libsds.dylib),
  run-macos launch OK (pre_login.log 0 errors), SIGTERM clean exit.
- Host static + iOS libsds: 9 _Sds* exports each, 0 leaks; iOS archive
  otool-verified platform IOS minos 16.2 (iPhoneOS26.5.sdk).
- Stamp: no-op rebuild 8s, nimble.paths + client mtimes unchanged, zero
  setup re-runs; restoring vendor/nim-sds does not re-trigger setup.

Gotchas for next agents (0008+):
- ALWAYS QMAKE=~/Qt/6.11.0/macos/bin/qmake USE_SYSTEM_NIM=1 for desktop
  makes (bare make mis-detects mkspecs -> linux paths in status-keycard-qt).
- libsds artifacts for the default flow are at
  vendor/status-go/.sds-build/{build,library} (NIMSDS_BUILD_ROOT); the
  scratch is wiped+recopied per sds task run — platform flips self-heal.
- Clean-store `nimble setup` takes ~6 min INCLUDING an unavoidable solve
  (lock fast path unreachable on 0.22.3); a ~10 min run means real trouble.
- The uuids/isaac fork pins are load-bearing for clean stores: repointing
  them back upstream reintroduces the INI wall.

Follow-ups recorded in the issue: drop uuids/isaac long-term; 4 upstream
nimble issues; pin moves to logos-messaging merge SHA when PR #85 lands.

=== 2026-07-06 — Issue 0008: status.nims app driver — DONE (pass: true) ===

Picked up: docs/superpowers/issues/0008-status-nims-app-driver.md
(PRD: docs/superpowers/prds/2026-07-06-one-command-and-develop-mode-prd.md)

Phase A (spike + driver, before the 0007 gate):
- Flag-forwarding spike (nim 2.2.4, scratch dir): the agreed UX
  `nim app status.nims --os:ios --cpu:arm64` works as designed — flags AFTER
  the .nims file are neither consumed nor validated by nim and arrive
  verbatim in argv (nimscript has no commandLineParams; use paramStr).
  Flags BEFORE the file are consumed (flip the script's own defines) but
  stay argv-visible → driver honors both placements. `run` task name safe
  (builtin is `r`). Full findings in the issue file.
- status.nims (repo root): `app` (host default; --os:ios/--os:android +
  --cpu dispatch), `run` (host-only), develop/undevelop/vendors fail-fast
  stubs → 0009. Delegation-only (make frozen): make nim_status_client /
  mobile-build / run with -j<ncpu> (matches CI MAKEFLAGS). Bootstrap
  inherited from make's nimble.paths stamp + .DEFAULT submodule init — NO
  second stamp scheme. Fail-fast kit validation per target: QMAKE XSPEC
  cross-check (catches ambient Android-QMAKE-on-desktop), IPHONE_SDK
  (+QMAKE_DEVELOPMENT_TEAM for iphoneos, --cpu/SDK contradiction check),
  ANDROID_SDK_ROOT/ANDROID_NDK_ROOT, Android --cpu vs per-ABI-kit check;
  driver sets ARCH from --cpu and defaults GRADLE_TARGETS=assembleDebug.

Phase B verification (post-0007 graph a30e7706e5; record in issue file):
- Wiped store + stamps → `nim app status.nims` = runnable host build in
  6:25 (setup → .sds-build libsds → client); `nim run status.nims`
  launched StatusDev.app.
- iOS leg 2:45: signed Status.app (codesign deep/strict OK, team
  8B5X2M6H2Y); Android leg 3:28: debug APK, all 5 native libs arm64-v8a.
  Platform sentinels fired on every flip (darwin→ios→android→darwin).
- Missing-env failures all ~0.8 s (<5 s budget), naming exact variables.
- No-op: driver 7 s vs bare make 5 s → driver overhead ≈2 s (eval 0.74 s).
  Desktop no-op FORCE chain is already seconds; mobile no-op FORCE chain
  70 s warm (0010 stamp-skip target).
- make targets unchanged: direct nim_status_client / mobile-build /
  run-macos all pass (delegation-only driver, zero make edits).

Gotchas for next sessions:
- status.nims eval walks parent configs: repo config.nims echoes "Building
  for macOS" (twice in nested worktrees) during driver startup — cosmetic.
- zsh: `VAR=x eval "cmd"` does not reliably export to the child — env
  prefixes must go on the real command, not on `eval`.
- Killing the launched app makes `make run`/`nim run` exit non-zero (exec
  raises on make's failure) — expected; a normal user quit exits 0.

Carry-overs: BUILDING.md front-door mention once 0009/0010 settle the
vendor UX; `run` stays host-only until develop-mode iterations; standing
0003/0004/0007 carry-overs unchanged.

=== 2026-07-07 — Issue 0009: develop-mode core (overlay + develop/undevelop/vendors) — DONE (pass: true) ===

Picked up: docs/superpowers/issues/0009-develop-mode-core-overlay.md
(PRD: docs/superpowers/prds/2026-07-06-one-command-and-develop-mode-prd.md;
mechanism: ADR 0007 nimble.paths overlay)

What was built:
- status.nims: vendor table (statusgo + sds rows; flavor/developBranch/FORCE
  fields ready for 0011 cmake + 0012 seaqt smo-6.4/nimqml), develop/
  undevelop/vendors tasks, applyOverlay internal task, develop-mode gating in
  app/run. Pins parsed live from the owning manifest (0010's URL flip needs
  no driver change). Overlay file = nimble.overlay (gitignored).
- Makefile: overlay joins the setup-stamp key ($(wildcard)); stamp recipe
  runs `nim applyOverlay status.nims` after every nimble setup (delegation
  only — bare-make regeneration covered). Derived copies inherit via the
  existing cmp rules, untouched.
- Divergence guard = checkout manifest vs `git show <pinRev>:<manifest>`
  (store-wipe-immune; skipped for interim file:// pins); runs per-build in
  the driver AND in applyOverlay; message carries the full file:// escape
  hatch + walls. Driver also self-heals a manual-`nimble setup`-clobbered
  nimble.paths (detect un-applied overlay -> re-invalidate stamp).
- Gating (ADR-0003 arm): sds => touch derived nimble.paths (FORCEs the sds
  engine; in-place branch fires for the overlaid path) + NEW cmp-mirror of
  in-place artifacts into .sds-build (statusgo.nims, submodule commit
  95166a5ab) so every Makefile keeps reading ONE layout; statusgo => rm
  libstatus.* (re-delegates the incremental sub-make) + REBUILD_NIM=true.
  Default mode pays nothing (no-op still ~8 s).

Verification (all 5 criteria; full record in the issue file): sds cycle
in-place build observed + undevelop refusals (dirty, then unpushed; --force)
+ post-cycle libsds BYTE-IDENTICAL to baseline; statusgo Go+wrapper probes
both picked up (rm-FORCE + REBUILD_NIM observed); divergence loud on both
driver and bare-make paths, revert unblocks; overlay survives lock-touch
regeneration incl. derived copy (cmp identical); git status identical
before/after modulo this issue's own edits.

Walls hit (documented in vendor/status-go/AGENTS.md):
1. NEW: warm re-setups emit dangling srcDir entries for srcDir-HOISTED store
   copies (isaac): materializing setup stores hoisted + emits root entry;
   every warm re-setup emits <root>/src which doesn't exist -> first client
   recompile after a warm setup dies ("cannot open file: isaac").
   Pre-existing, exposed by 0009's REBUILD_NIM arm. Fixed in config.nims'
   isaac hack (points at whichever of entry / entry-/src exists).
2. Known nested-worktree parent config.nims rpath leak (parent on
   release/2.38.x uses pre-rename STATUSKEYCARDGO_LIBDIR; empty -> bare
   -rpath -> ld mmap error on bin/StatusQ). Fixed with the proven Makefile
   alias export (same fix as the seaqt worktree).

Gotchas for next sessions (0010+):
- Desktop statusgo-shared-library output is NOT byte-reproducible (no
  -ldflags=-buildid= there, unlike the 0004-fixed mobile recipes) — relevant
  to 0010's stamp-skip arm.
- Develop-mode FORCE costs: statusgo ~2:30 warm/build (sub-make + client
  recompile); sds no-op ~6 s. Bare make covers regeneration + apply-time
  guard; per-build FORCE/guard arms are driver-only (nim app is the door).
- undevelop unpushed check = log --branches --not --remotes over ALL local
  branches; no-remote checkouts always refuse (use --force).

Carry-overs: standing 0003/0004/0007/0008 items unchanged; BUILDING.md
front-door + develop-mode docs once 0010 settles the statusgo pin.

=== 2026-07-07 — Issue 0010: status-go full-pin (submodule removed, URL#hash pin, stamp-skip) — DONE (pass: true) ===

Picked up: docs/superpowers/issues/0010-statusgo-full-pin.md
(PRD: docs/superpowers/prds/2026-07-06-one-command-and-develop-mode-prd.md)

What was built:
- nim_status_client.nimble: statusgo file:// requires → URL#hash pin
  (status-im/status-go.git#d9281bce9, branch nimble-phase1-pin). Pin bump =
  one-line #hash edit.
- NEW WALL (grilled; user approved the fix): nimble 0.22.3 builds dependency
  binaries unconditionally at store materialization (vnext gates on
  bin.len>0 alone; hook-cancel aborts the setup; hybrids/installExt only
  whitelist files — and ANY whitelist strips go.mod/Makefile/*.go from the
  store copy). Fix: statusgo.nimble went SOURCE-ONLY on nimble-phase1-pin
  (d9281bce9: bin/installDirs/installFiles/hook dropped). status_backend
  still builds from checkouts via statusgo.nims. Both walls in status-go
  AGENTS.md. Upstream ask to file: skip dep bin builds.
- Scratch engine: read-only store copy → writable .statusgo-build/ at repo
  root, owned by `nim prepareStatusgo status.nims` (in status.nims): wipe+
  copy keyed on (store path, --key flag set); triple flips stay with the
  platform sentinel. Stamp-skip default arm: pin unchanged + artifacts
  present ⇒ status-go sub-make and sds task NOT invoked at all. Developed
  statusgo keeps the 0009 FORCE arms in the checkout.
- Both Makefiles + mobile Common.mk derive STATUSGO_ROOT (scratch vs
  checkout) from nimble.overlay; every artifact path (STATUSGO_LIBDIR,
  NIMSDS_*, stub-bindings gens, version) rides it — no direct
  vendor/status-go references remain in the build system.
- Version stamping (gitless store copy): STATUSGO_VERSION = store entry's
  nimblemeta.json vcsRevision (pinned) / git describe (developed). go.mod
  local replaces: none exist on this branch — non-issue.
- Mode flips (develop/undevelop) drop bin/nim_status_client (desktop rpath
  moves between scratch and checkout); 24h force-rebuild pre-clean is
  develop-only now.
- Submodule vendor/status-go removed (staged; backup ref
  backup/nimble-0010-statusgo, dir preserved at .phase2-vendor-backup/
  status-go with a working git dir). Develop mode materializes a fresh
  clone from the pin URL — exercised for real.

Verification highlights (full record in the issue file):
- Wiped store → make nimble-deps 6:31 → nim app → launch (lsof: libstatus +
  libsds loaded from .statusgo-build) → clean exit.
- No-op stamp-skip: desktop 5.9 s, iOS 15.1 s, Android 15.2 s (0008
  baselines: 7 s / ~70 s / ~70 s), bare make 5.9 s; zero sub-make banners.
- Pin bump (content-changed hash): new store entry, scratch auto-refresh,
  rebuild with no manual cleaning, both directions; empty-commit bump
  correctly no-ops (content-addressed store).
- develop cycle from NO checkout: clone at pin (branch develop), overlay
  build, Go probe picked up (cbindings-generated symbol), undevelop dirty
  refusal, post-undevelop byte-identical pinned artifact + zero sub-makes.
- iOS device (signed) 2:47 / Android APK (all 5 native libs) 3:30 from the
  pinned store copy; sentinel fired on every platform flip under the new
  paths.

Gotchas for next agents (0011/0012):
- statusgo vendor row is the template: pinManifest/forceTouch support the
  "@statusgo/" active-root marker; sds's pin manifest resolves from the
  store copy when no checkout exists.
- Any warm re-`nimble setup` rewrites nimble.paths with the isaac srcDir
  variant (0009 wall) → one cosmetic libsds re-run (~10 s) per re-setup.
- vendor/status-go/AGENTS.md lives in the (removed) submodule: read it from
  .phase2-vendor-backup/status-go, the store copy, or a develop checkout.
- Dependency manifests must NEVER declare bin (or any install whitelist) —
  the 0010 wall; check before pinning new vendors.

Lock finding: `nimble lock` regen is a net negative — URL#hash packages
(statusgo + transitives) never enter the lock at all on 0.22.3, and the
regen writes isaac vcsRevision="" (INI wall artifact) + drifts snappy.
Committed lock kept (it never constrains solves per the documented wall).

Carry-overs: file the upstream nimble issue (dep bin builds); delete
scripts/bump-status-go.sh with CI migration; standing 0003/0004/0007/0008/
0009 items unchanged.

=== 2026-07-07 — Issue 0011: keycard pair (FetchContent pins + develop redirect) — DONE (pass: true) ===

Picked up: docs/superpowers/issues/0011-keycard-pair-fetchcontent.md
(PRD: docs/superpowers/prds/2026-07-06-one-command-and-develop-mode-prd.md;
phase A survey committed 1417cd5dd6 before the 0010 gate, phase B after)

What was built:
- cmake/status-keycard-qt/CMakeLists.txt: app-owned FetchContent wrapper owns
  the status-keycard-qt pin (GIT_TAG a6cbdd05 = the removed submodule SHA);
  keycard-qt pin stays owned by status-keycard-qt's CMakeLists (audit passed:
  df00b931, already a fixed SHA). Wrapper forces CMAKE_*_OUTPUT_DIRECTORY to
  the build root — as a subproject the lib otherwise lands in
  _deps/status-keycard-qt-build/ and breaks every path contract (found in
  verification: make does not check a recipe produced its target).
- Makefile: -S → wrapper, build dir → build/status-keycard-qt/<platform>
  (out of the checkout, mode-independent), develop state derived from
  nimble.overlay (0010 STATUSGO_ROOT pattern), FETCHCONTENT_SOURCE_DIR_
  {STATUS-KEYCARD-QT,KEYCARD-QT} ALWAYS passed (empirically: empty cache
  value == unset, omitted -D == sticky → always-pass kills stale redirects).
- mobile: wrapper -S + own cmake subtree BUILD_PATH/status-keycard-qt (old
  root cache would refuse the source flip) + overlay-derived redirects +
  developed-checkout file tracking. Converted but NOT build-verified
  (desktop-only acceptance) — flag for the next mobile matrix run.
- status.nims: vfCmake rows (status-keycard-qt, keycard-qt), cmake pin parser
  (@keycard-parent sentinel: checkout → _deps → graceful degradation in
  vendors), develop/undevelop legs, invalidateOnModeFlip (rm cmake artifacts
  on BOTH flips — undevelop especially, nothing else re-runs the recipe),
  per-build FORCE arm = rm lib; divergence guard n/a for cmake (redirect
  makes cmake read the checkout's own CMakeLists — no silent-drift mode);
  applyOverlay/overlayApplied skip cmake rows.
- Submodule removed (Phase 2 staged playbook): backup ref backup/nimble-0011-
  pin, functional git dir at .phase2-vendor-backup/status-keycard-qt
  (core.worktree repointed). BUILDING.md knob docs updated.

Verification (all 5 criteria; record in the issue file): default build with
no checkout — _deps at exact pins, no-op 6.4 s with recipe not invoked,
OFFLINE reconfigure+rebuild rc=0 under dead proxy (SHA GIT_TAG = no per-build
fetch, no FETCHCONTENT_UPDATES_DISCONNECTED needed); parent develop cycle —
redirect in CMakeCache, checkout compiled, symbol probe in dylib in a 6.9 s
incremental build, undevelop refusals (dirty → unpushed → --force), return
build BYTE-IDENTICAL to baseline (8b40474c); nested keycard-qt develop ALONE —
parent stayed pinned (redirect empty) while nested string probe surfaced in
the parent dylib, exit byte-identical again; smoke — StatusKeycardQt C API
callback + StatusKeycardContextImpl + PC/SC channel init in the launched app;
vendors lists all four with pin+state incl. clean-clone degradation. Client
relinked once post-conversion (rpath → build/status-keycard-qt/macos).

Gotchas for 0012:
- Subproject output-dir trap above (wrapper must pin CMAKE_*_OUTPUT_DIRECTORY).
- FETCHCONTENT_SOURCE_DIR_<NAME> casing = TOUPPER with hyphens KEPT
  (…_STATUS-KEYCARD-QT); it is a CACHE var — always-pass (empty = pinned) is
  the stale-proof discipline.
- cmake develop/undevelop rewrites nimble.overlay → one ~50 s nimble setup on
  the next build although cmake vendors aren't in the nimble graph (uniform
  stamp machinery; accepted).

Carry-overs: mobile keycard legs unverified (above); Windows path derivations
unchanged but unvalidated (out of scope). Remaining: 0012 seaqt conversion.

=== 2026-07-07 — Issue 0012: seaqt pair conversion (URL#hash pins, submodules removed) — DONE (pass: true) ===

Picked up: docs/superpowers/issues/0012-seaqt-pair-graph-adoption.md
(PRD: docs/superpowers/prds/2026-07-06-one-command-and-develop-mode-prd.md;
spike record: specs/2026-07-06-seaqt-graph-spike.md; grilled 2026-07-07
decision: PASS path at the current submodule SHAs, seaqt developBranch
smo-6.4 — pin bumps are a separate later decision.)

What was built:
- nim_status_client.nimble: requires seaqt/nim-seaqt#2d95808 (branch
  smo-6.4, tag qt-6.4-seaqt-gen-5bc1bc58 points at it) + nimqml/
  nimqml-seaqt#c5e5831. Pure-source packages: C++ shims {.compile.} into
  the client nimcache straight from the read-only store — no scratch
  engine, no sub-build. Lock NOT regenerated (0010 finding: URL#hash pkgs
  never enter it; regen is net-negative).
- config.nims: the two hardcoded vendor path switches DELETED (nimble.paths
  carries the roots); isaac srcDir-hoist hack generalized to nimqml
  (declares srcDir "src", store copy hoisted — same wall class). NOTE
  empirical nuance: warm re-setups flip isaac (transitive range pick) to
  the dangling /src variant but seaqt/nimqml (direct root URL#hash
  requires) KEEP root entries — the nimqml hack arm is a safety net.
  seaqt_compat include unchanged (app-owned; global passC reaches
  {.compile.}'d store sources, desktop AND ios kits).
- status.nims: vfNimbleGraph rows seaqt (developBranch smo-6.4) + nimqml
  (master), both clientRebuild=true (REBUILD_NIM is the whole FORCE arm).
  NEW Vendor.srcDir field: the overlay rewrite remaps a hoisted store ROOT
  entry onto <checkout>/<srcDir> (nimqml store = hoisted, checkout = src/)
  — observed: applyOverlay emits vendor/nimqml-seaqt/src.
- Submodules removed x2 (staged playbook): backup branches
  backup/nimble-0012-pin; functional git dirs at
  .phase2-vendor-backup/{nim-seaqt,nimqml-seaqt} (core.worktree
  repointed). Playbook gotchas recorded: stage .gitmodules BEFORE git rm
  --cached; once the dir moved, write core.worktree via git config --file
  <gitdir>/config (git -C refuses while the worktree pointer dangles).
- BUILDING.md: seaqt-pair section; submodule list = C/C++-only now.

Verification (all criteria; full record in the issue file):
- Wiped store + NO checkouts: nim app = 9:10 end-to-end (clean solve incl.)
  -> launch smoke OK (QML up, keycard init, libs from pinned roots, clean
  SIGTERM); storybook-build 1:35; no-op 6.3-7.7 s. Client compile provably
  store-sourced (nimcache json: 142 seaqt + 2 nimqml store refs, 0 vendor).
- develop nimqml: clone at pin (master), overlay remap to /src observed,
  compat-layer probe compiled into the linked unit (nm: T
  _nimqml_0012_probe; LTO internalizes it out of the exe), client
  relinked; undevelop dirty-refusal -> revert -> clean exit -> pinned
  rebuild; cmp NOT byte-identical (+64 B, fresh LC_UUID — Mach-O mints a
  UUID per relink, stabs track nimcache mtimes; inputs = identical store
  paths; desktop client was never relink-reproducible, 0009 note).
- iOS device leg: driver build 2:33 from the store — 222 fresh @pseaqt
  units; gen_qvariant.cpp.o bitcode embeds arm64-apple-ios17.0.0 + the
  store source path (decisive); codesign --verify --deep --strict OK.
- Divergence guard picks the right manifest: develop seaqt -> branch
  smo-6.4 @ 2d95808; seaqt.nimble edit fails the build in seconds naming
  seaqt.nimble + pin + revert cmds + file:// escape hatch.
- vendors lists all SIX vendors with pins + state; platform sentinel fired
  on the ios->desktop flip-back.

Gotchas for next sessions:
- Shared-nimcache archaeology: mobile compiles reuse nimcache/ at the repo
  root; pre-conversion @m..@svendor@snim-seaqt units linger inert next to
  the live @pseaqt (path-root-relative) units — grep for @pseaqt/@pnimqml,
  not pkgs2, when checking mobile source origins.
- Parent-checkout worktree nesting: the parent repo's stale nimble.paths
  (different ~/.nimble nimqml copy) sits on nim's parent-config walk;
  empirically the worktree resolution wins (desktop json + ios bitcode
  both show our store) — watch shadowing if the parent grows seaqt pins.
- Mobile nim lib rule prereqs don't include nimble.paths/config.nims — a
  pin/config change alone doesn't rebuild libnim_status_client; drop the
  lib (or touch a src file) to force it.

Iteration status: 0007-0012 ALL DONE. Default mode has zero vendor
checkouts (six pinned vendors, two flavors); the remaining submodules are
C/C++-only. Carry-overs: Android seaqt leg unexercised (committed .pc tree
exists); pin bumps (qt-6.11 line) = separate decision; standing 0003/0004/
0007-0011 items unchanged.

=== 2026-07-07 — ITERATION 2 COMPLETE ===
All six issues done (0007-0012). End state verified: one-command build
(`nim app status.nims [--os --cpu]`, no-op ~6s desktop / 15s mobile),
per-vendor develop mode (overlay + FetchContent redirect), six vendors
pinned (statusgo d9281bce9, sds 5c89d61/PR85, seaqt smo-6.4@2d95808,
nimqml c5e5831, status-keycard-qt a6cbdd05, keycard-qt df00b931), FOUR
submodules removed this iteration (status-go, status-keycard-qt, nim-seaqt,
nimqml-seaqt; backups in .phase2-vendor-backup with functional git dirs).
`nim vendors status.nims` lists all six with pin + state.
Upstream-asks consolidated in docs/superpowers/upstream-asks.md (9 nimble
issues to file, sds PR85 merge, status-go PR to develop, uuids/isaac fork
retirement, seaqt tagging).
Follow-on tracks (next grill sessions): NBS deletion (original Phase 3),
0006 sds target-subdir artifact packages (upstream-gated), CI migration to
the driver, BUILDING.md/onboarding polish, Windows validation, true
`nimble install` desktop layer, seaqt qt-6.11 pin-bump evaluation.

=== 2026-07-08 — Issue 0013: nimble-native build/run OOTB — DONE (pass: true) ===

Picked up: docs/superpowers/issues/0013-nimble-native-build-run.md
(PRD: docs/superpowers/prds/2026-07-06-one-command-and-develop-mode-prd.md;
decisions grilled 2026-07-07: one store = default ~/.nimble)

What was built:
- status_env.nims (new shared include): qmake discovery (single -query
  dump), kit hint, overlay parsing, statusgo build root — one source for
  the driver AND config.nims.
- config.nims now owns the desktop client's FULL flag set (phase A):
  mm/malloc/outdir/flavor/version defines/all link inputs/rpaths (now incl.
  libsds + StatusQ cmake libdir → DYLD-free bare exec), env-or-derived
  (make env wins; STATUS_BUILD_ENV_ASSERT=1 = bring-up parity assert,
  passed on full make builds). seaqt's pkg-config env self-served via
  putEnv when make didn't export it. Make client recipe thinned to a bare
  `nim c src/nim_status_client.nim` (mac/linux; Windows keeps legacy flags)
  — every front door shares one flag set by construction.
- nim_status_client.nimble: binDir="bin", `before build` hook → `nim
  buildArtifacts status.nims` → make client-deps (everything except the
  client compile), skip switch STATUS_SKIP_BUILD_ARTIFACTS=1 as successful
  no-op, exec-failure → explicit return false (execHook swallows hook
  script failures otherwise — verified in nimble source); `after build`
  hook mirrors make's install_name fixups.
- Phase D: APP_NIMBLE_DIR retired; `nimble setup` runs against ~/.nimble
  (NIMBLE_DIR env = CI/clean-room override); BUILDING.md migration note.

Empirics that shaped it (issue file, "Pre-work empirics"): nimble vNext bin
compile contract captured (store nim, --noNimblePath, -d:release forced,
--path per solution pkg, cwd=pkgdir, env inherited); config-path BEATS
nimble CLI path (overlay composes); CLI define beats config define;
putEnv-in-config reaches module gorge; binDir lands make's exact path.

Incidents found + fixed during verification (all recorded in the issue):
1. Default-store pkgcache (polluted 2026-07-07) solved a DIFFERENT graph
   (libp2p 1.15.3/websock 0.3.0/lsquic 0.0.1) — carried the retired
   store's known-good pkgcache over + purged 3 same-version/different-rev
   entries; resolution now matches the verified graph exactly.
2. Nimble's CLI --path:<src> is processed BEFORE configs → an ENCLOSING
   checkout's nim.cfg outranks it and the compile silently builds the
   PARENT's sources (the motivating "parent source leakage", mechanically
   understood) — config.nims now excludePath's every ancestor checkout src.
3. Release-define ordering: -d:release set AFTER debugger:native silently
   stripped make's debug map (133k vs 415k syms) — flavor now set first.
4. Missing MACOSX_DEPLOYMENT_TARGET at nimble link flipped ObjC section
   placement — env fallback added.

Verification (all 7 criteria; record in the issue): clean-room fresh clone
+ EMPTY store (NIMBLE_DIR=~/.nb0013) + kit env only → nimble build 9:54 →
launch; byte-parity make↔nimble = identical __text/__const/__cstring/
symbols, residue only LC_UUID + 839 OSO stab mtimes + signature (make↔make
relink with unchanged .o = 0 bytes); hook no-op 6.55 s vs dispatch tax
~78 s (upstream ask #4); fail-fast kit error ~1–2 s past dispatch with
exact-variable message; develop compose proven with an sds probe through
nimble build (overlay applied in-hook, probe in artifact, clean return to
pin); stale-store immunity: 21/21 compile store entries ∈ nimble.paths
with a whole divergent graph deliberately present in pkgs2; iOS leg 139 s
signed + codesign strict OK.

Gotchas for next sessions:
- Nested worktrees: in-tree nimble build/run needs QT_LIBDIR/STATUSGO_
  LIBDIR/STATUSKEYCARD(GO)_LIBDIR/STATUSQ_INSTALL_PATH exported (the
  PARENT checkout's old config emits bare -rpath without them). Normal
  clones need nothing (clean-room proof). Out of scope per decisions.
- Clean-room second-invocation hiccup (one-time, self-healing): bootstrap
  pass left a drifted .statusgo-origin → next build's prepareStatusgo
  refreshed the scratch mid-run while make's stat cache skipped the
  status-go rebuild → one link failure; third build green + stable.
  Follow-up: bootstrap-ordering look at prepareStatusgo origin writing.
- nimble always re-runs the client compile (needsRebuild=true for
  build/run) — no-op nimble build ≈ 89 s total; `nim app` stays the fast
  daily path (5.9–7 s no-op).
- Standing risk (pre-existing): fresh machines can solve a different graph
  (lock gates nothing, URL#hash roots force full solves, pkgcache tables
  drift) — upstream asks #4/#6/#8.

Carry-overs: file the bootstrap-ordering follow-up; standing 0003–0012
items unchanged; upstream asks doc already current.

=== 2026-07-08 — Issue 0014: prl-to-pc into the nimble graph (#v0.2.0 tag pin) — DONE (pass: true; push pending) ===

Picked up: docs/superpowers/issues/0014-prl-to-pc-graph-adoption.md
(PRD row 0014; decisions grilled 2026-07-08: tag pin #v0.2.0, source-only
manifest, pinned transitive deps, staged submodule removal)

What was built:
- prl-to-pc commits (local branch fix/lockfile-nimblepath -> f649ba6;
  annotated tag v0.2.0 RE-POINTED there — never pushed, so legitimate):
  manifest drops bin AND srcDir + pins regex/unicodedb by SHA;
  qt-pkgconfig.mk gains consumer-overridable QT_PC_BUILD_DIR +
  QT_PC_CONSUMER_PATHS and a store-copy generate REFUSAL
  (nimblemeta.json marker).
- App: requires status-im/prl-to-pc.git#v0.2.0 (the graph's FIRST
  version-tag pin); prlToPcRoot() in status_env.nims (overlay-aware root,
  shared config.nims/make); config.nims reads .pc trees from the resolved
  root, wrapper from repo-local .prl-to-pc-build/.pcwrap; Makefile
  includes $(PRL_TO_PC_ROOT)/qt-pkgconfig.mk with an include-remake-reexec
  bootstrap for fresh clones; vendor row prl-to-pc (developBranch main) +
  new flipRemove FORCE arm (mode flips drop the repo-local tool binaries).
- Submodule removed (staged playbook); backup FUNCTIONAL at
  .phase2-vendor-backup/prl-to-pc — the ONLY holder of the branch+tag
  until the user pushes. BUILDING.md updated; upstream-asks gains the
  prl-to-pc push item.

Empirics that shaped it:
- #v0.2.0 annotated-tag pin RESOLVES on nimble 0.22.3 (loopback spike):
  specialVersions ['#v0.2.0','0.2.0'], vcsRevision = peeled commit. The
  pre-identified tag-ref grill trigger never fired.
- NEW WALL SHARPENING: a srcDir-declaring package's store copy is hoisted
  and STRIPPED to srcDir contents (nimqml/regex evidence + spike) — bin
  AND srcDir must both be absent for full-tree store copies (statusgo
  precedent). The issue's "hoist wobble doesn't matter" assumption was
  wrong in a load-bearing way.
- Pre-push bring-up: the brief's overlay-only sketch is structurally
  impossible (unresolvable require fails nimble setup BEFORE overlay
  application); instead the pkgcache clone
  (~/.nimble/pkgcache/githubcom_statusimprltopcgit_v020) is seeded from
  the backup repo — nimble reuses cached clones, store checksums are
  content-addressed and URL-independent, so post-push machines
  materialize the identical entry (d902f8c9). Post-close addendum
  2026-07-08: README rewritten to document the real shape (three layers +
  mk knobs + store-copy semantics); tag re-pointed to f649ba6, entry
  re-verified (full build 1:09.9, no-op 5.9 s).

Verification (record in the issue): default build with NO checkout +
wiped entry = 1:09.95 (known-good graph held); no-op 6.95-7.33 s; nimble
build (hook) 1:37 warm rc=0 + bare-exec launch smoke clean; store entry
per-file md5+mtime IDENTICAL across a full build (tools in
.prl-to-pc-build); generate-refusal fires from the store copy; develop
round-trip (checkout from backup, mk probe consumed, applyOverlay 1
entry, divergence guard fired on tag-shaped rev, undevelop restored the
pin); vendors lists 7; iOS leg 2:29.6 signed + codesign strict OK;
desktop flip-back 54.5 s. Post-push flip command recorded (NOT run —
push didn't happen in-session).

Gotchas for next sessions:
- Do NOT delete ~/.nimble/pkgcache/githubcom_statusimprltopcgit_v020
  before the push — it is what keeps the #v0.2.0 solve alive.
- develop prl-to-pc pre-push: materialize the checkout from
  .phase2-vendor-backup/prl-to-pc (fresh GitHub clones lack the tag).
- vendors/develop pin display shows "(HEAD <sha>; pin v0.2.0)" for tag
  pins (string compare) — cosmetic.
- A 12.5 h orphan nim_status_client from the 0013 overnight session held
  the single-instance lock; killed during the launch smoke.

Carry-overs: user push of prl-to-pc branch+tag, then run the post-push
flip; standing items unchanged (NBS deletion Phase 3, CI migration,
seaqt qt-6.11 evaluation, 0013 bootstrap-ordering follow-up).

--- 2026-07-09 — 0014 post-push flip (carry-over closed) ---
User pushed prl-to-pc: tag v0.2.0 (peeled f649ba6) + branch live on
status-im/prl-to-pc; PR #1 MERGED to main. Flip run: seeded pkgcache +
store entry + nimble.paths moved to ~/.nimble/bak-0014-postpush/
(classifier blocked rm; mv = reversible wipe) → make nimble-deps
re-solved #v0.2.0 from GitHub in 1:10 → IDENTICAL store entry
prl_to_pc-0.2.0-d902f8c9… (vcsRevision f649ba6, specialVersions
['#v0.2.0','0.2.0'], per-file md5 byte-identical to the seeded one,
proving the content-addressed/URL-independent claim on the real remote).
nim app 10.4 s, no-op 6.3 s, vendors row clean. The seeded-pkgcache
DO-NOT-DELETE gotcha is RETIRED. Residual: upstream main is 2 commits
past the tag (probe-mode merge — the qt-pkgconfig-mode work is now
upstream — + 81aa1e8 space-safe consumer-paths review fix); optional pin
bump when a v0.2.1 tag appears. Backup dir ~/.nimble/bak-0014-postpush
deletable freely.
Remaining carry-overs: NBS deletion (Phase 3), CI migration, seaqt
qt-6.11 evaluation, 0013 bootstrap-ordering follow-up, 0006
(upstream-gated), status-go nimble-phase1-pin → develop PR, sds PR#85 +
uuids#15 still open (isaac#4 merged).

=== ITERATION 3 DESIGNED 2026-07-09 (grill session) ===
PRD docs/superpowers/prds/2026-07-09-nimble-owns-nim-compilation-prd.md.
Goal: nimble/nimscript own ALL nim compilation; make demoted to packaging +
CI helpers + interim mobile; NBS deleted; prl-to-pc consumed nimble-native.

Decisions (grilled):
- Dev flow 100% make-free (option c). make survives for pkg-*, signing,
  flatpak, storybook, mobile interim, CI helpers. Invariant: root Makefile
  never invokes nim (grep-checked); mobile Makefile joins at its follow-on.
- prl-to-pc gains an UPSTREAM nimscript interface qt_pkgconfig.nims run via
  `nim e` (env/tools/generate). MECHANISM FINDING: include/import resolve at
  PARSE time but nimble.paths --path switches apply at script RUNTIME → a
  dynamic store path can never be included; it must be executed. Driver caches
  `env` output into a repo-local gitignored file; config.nims reads it (no
  subprocess per nim invocation) and STOPS duplicating kit derivation.
  Ships as v0.3.0 off main (probe-mode + space-safe fix already there);
  qt-pkgconfig.mk RETAINED (dual interface). PUSH GATE known up front.
  pkgcache key embeds the ref (githubcom_statusimprltopcgit_v020) → a new tag
  pin mints a fresh clone; staleness wall (ask #8) does not bite.
- TOOLCHAIN (spiked 2026-07-09): with PATH scrubbed of ~/.nimble/bin AND NBS,
  `nimble setup` still resolves+uses pkgs2/nim-2.2.4/bin/nim, and
  `nimble shellenv` emits that bin dir. => machine prerequisite is NIMBLE
  ONLY. Bootstrap: `nimble setup && eval "$(nimble shellenv)"`. No version
  guard needed (user call) — a bootstrapped shell cannot drift. Internal
  `exec nim` calls resolve the STORE nim (incl. `NIM :=` for interim mobile
  make legs, which run on nimble-only CI agents).
- Artifacts: status_artifacts.nims, one generic cmake proc (StatusQ,
  DOtherSide, keycard wrapper, translations — cmake's own incrementality is
  the no-op path); QR-Code-generator is NIM-ONLY (no StatusQ consumer) →
  {.compile.} in its existing wrapper, kill the .a + passL + platform-cleanup;
  rcc + bottles + submodule bootstrap + setup-stamp absorbed; TWO gating
  patterns only: stale(outputs,inputs) + the .statusgo-build key-file.
  compile-translations = task, not a build step.
- nimterop REJECTED (dormant since 2023-04; cCompile discouraged by its own
  docs; bindings are handwritten; stdlib {.compile.} covers the need).
- DOtherSide: dead on origin/master (d3b8917a04) → zero investment, rides the
  generic cmake proc until master merges.
- REBUILD_NIM replaced: developed vendor ⇒ skip stale() gate + always compile;
  human `--force` flag. .update.timestamp/NIM_SOURCES/deps/update deleted.
  `make update` NOT kept as a shim.
- Windows NIM_PARAMS branches ported into config.nims NOW (ported, unverified
  — 0012 Android-leg precedent). run-* make targets deleted.
- CI (user call): NIMBLE-ONLY agents; pipelines use `nimble build` + nimble
  task aliases; Jenkinsfiles updated THIS iteration, verified post-push
  (checklist, not a pass claim). choosenim/nim-version.sh rejected.
- Slices: 0015 prl-to-pc nimble-native (first — push gate is the long pole)
  → 0016 buildArtifacts native → 0017 driver owns nim compiles → 0018 NBS
  deleted (gate: nim-free machine proof) → 0019 CI. Every slice leaves
  `nim app` AND `nimble build` green.
- CONTEXT.md amended: the no-checkouts end-state scopes to VENDORS; the 5
  C/C++ submodules are pins, not Vendors.

--- 2026-07-09 — SPIKE (orchestrator-run; de-risks 0018/0019) ---
The spike SUBAGENT produced nothing and marked its task done; completion
rejected, task reopened, agent stopped. Orchestrator ran the spike itself.
Full report: scratchpad/spike-i3/REPORT.md. Real ~/.nimble never written
(cold tests via redirected HOME).

Q1 COLD STORE = YES. `nimble setup` with HOME redirected + PATH scrubbed of
~/.nimble/bin and NBS: succeeds, builds nim 2.2.4 (nimbinaries + csources_v3)
and ALSO downloads a prebuilt nim-2.2.10 as evaluator. 5:18 wall, 7.9 GB,
warm re-run 0.57 s. Store checksum IDENTICAL to the real machine's
(nim-2.2.4-b4bb510b…) → content-addressed, machine-independent.
=> 0019 must budget ~5 min + ~8 GB per fresh agent, or cache ~/.nimble.
Trap: ~/.nimble/nimbinaries survives a NIMBLE_DIR override — only a HOME
redirect gives a genuinely cold store.

Q2 = YES. `nimble build` with no nim on PATH builds and the binary runs.

Q3 = YES, and it REFUTES a PRD assumption: nimble INJECTS the pinned
compiler's bin/ into PATH for tasks AND for `before build` hooks. A bare
`exec "nim --version"` inside a hook prints 2.2.4 with no nim on the caller's
PATH. => the store-nim rule is unnecessary for anything nimble invokes; it is
needed only for things invoked OUTSIDE nimble (bare `make mobile-build` on a
nim-free agent). Simplest 0019 rule: CI reaches make through a nimble task
alias (or a shellenv shell), never bare.

Q4 = context-dependent; the obvious answer is WRONG in one VM:
  - manifest VM (tasks/hooks/aliases): findExe("nim") = pinned 2.2.4 ✅;
    selfExe()/querySetting(libPath) = nimble's EVALUATOR 2.2.10 ❌.
  - .nims run by nim (driver): getCurrentCompilerExe() ✅ (it lives in std/os
    as a compileTime proc — needs `import std/os`; status.nims:31 has it).
  - `nimble path nim` is UNUSABLE: prints TWO same-version entries
    (nim-2.2.4-9e6d4e22… specialVersions ['2.2.4'] and nim-2.2.4-b4bb510b…).
  LIVE HAZARD: status.nims calls getCurrentCompilerExe() and is include'd by
  the manifest. Via `nim <task> status.nims` correct; via `nimble <task>`
  (which 0019 mandates for CI) it yields the 2.2.10 evaluator. nimExe() must
  branch on context.

UNPLANNED FINDING — the divergence this migration exists to fix:
  NBS vendored nim (today's dev+CI PATH) = 2.2.10
  manifest pin                           = nim == 2.2.4
  ~/.nimble/bin/nim (shellenv/store)     = 2.2.4
So `nim app status.nims` compiles with 2.2.10 while `nimble build` compiles
with 2.2.4 — two compilers, one repo, right now. This weakens 0013's
make↔nimble byte-parity record (it compared two different compilers' output)
and is the strongest single argument for 0018. Iteration-3 briefs must stop
prepending the NBS nim to PATH.

Incidental: nimble rejects package names ending in "pkg".

================================================================================
2026-07-09  ISSUE 0015 — prl-to-pc nimble-native (implementer agent, run 1)
================================================================================
DONE except the push gate. prl-to-pc now publishes qt_pkgconfig.nims, an
EXECUTED consumer interface (`nim e <root>/qt_pkgconfig.nims <cmd>`), and the
app consumes it as the single source of truth for Qt pkg-config.

Upstream (prl-to-pc commit 4a31fc06, branch main, UNPUSHED; mirrored at
.phase2-vendor-backup/prl-to-pc backup/nimble-0015-v0.3.0):
  - qt_pkgconfig.nims: `env [buildDir]` / `tools <buildDir> <consumerPaths>` /
    `generate [buildDir [consumerPaths]]`. Owns kit derivation + the
    System/Generated probe. Refuses to generate into a store copy.
  - tests/test_qt_pkgconfig_nims.nim (10 tests) wired into `nimble test`;
    drives the same three fixture kits as the mk suite.
  - manifest -> 0.3.0. qt-pkgconfig.mk RETAINED, untouched (dual interface).

App:
  - status.nims: prepareQtPkgconfig() (app/run/buildArtifacts, host only) runs
    `tools`, then `env` ONCE, caching to .prl-to-pc-build/qt-pkgconfig.env keyed
    on (qmake path, resolved package root, kit). New task qtPkgconfigGenerate.
    flipRemove drops the tools + the cache on develop/undevelop flips.
  - config.nims: kit derivation and the wrapper assertion DELETED; it replays
    the cache (prepend semantics for PKG_CONFIG_PATH / PATH, idempotent).
  - Makefile: desktop's `qt-pkgconfig` prerequisites and the app's stub targets
    gone; the mk include SURVIVES for the interim mobile + nim-test-run legs.

VERIFIED (details in the issue's verification record):
  Generated kit 6.11.0 full build 97.9 s + launch smoke; System kit 6.11.1 full
  build 123.9 s with NO wrapper built at all (counter-proof: the OLD config.nims
  fails there — "the Qt pkg-config wrapper is missing"); default-mode build
  against a real store copy 65.5 s, store entry byte-identical (1161 files,
  per-file md5) before/after; no-op 6.78-7.25 s (0014: 6.95-7.33); nimble build
  132 s; iOS leg 147 s signed; develop round-trip + cache re-key both directions.

NEW WALLS (added to .phase2-vendor-backup/status-go/AGENTS.md):
  - nimscript has NO mtime API (os.getLastModificationTime is {.error.} there)
    -> gate on content keys (hash(src)+NimVersion+flags) or shell `test -nt`.
  - gorgeEx MERGES the child's stderr into the captured output -> validate the
    shape of anything you parse (the driver rejects non-KEY=VAL lines).
  - putEnv propagates to gorgeEx/exec children (portable probe, no env-prefix).
  - thisDir() is canonicalized (macOS /var -> /private/var); script args are not.
  - `nim e` on a script inside the repo NEEDS --skipParentCfg:on, or the parent
    walk hands it the app's config.nims (which reads the cache it is producing).
  - `nim applyOverlay` writes nimble.paths, which IS make's setup stamp: running
    it by hand marks the stamp fresh and the next build SILENTLY SKIPS
    `nimble setup`. Cost an hour: a pin bump appeared to resolve while the store
    still held the old entry.

AMENDED CRITERION: `env` on a System-mode kit is NOT empty — it still prints
PKG_CONFIG_PATH=<kit's own pkgconfig dir>. Without it pkg-config answers from
its built-in search path (brew's Qt at /opt/homebrew/lib on this machine). What
is empty is the generated machinery: no wrapper, no PATH prepend, no override.
The mk has always behaved this way; the dual interface must not diverge.

SCOPED CRITERION: `make qt-pkgconfig*` "no longer exists" = the APP's stub
targets are gone and the desktop build has no such prerequisite. The mk's own
targets survive with the include the mobile/nim-test legs still need.

PUSH GATE (human): from vendor/prl-to-pc — `git push origin main`, then
`git tag -a v0.3.0` at 4a31fc06 and `git push origin v0.3.0`. Push the commit
AS-IS (the app's interim pin names that SHA; a seeded pkgcache clone at
~/.nimble/pkgcache/githubcom_statusimprltopcgit_4a31fc06… is the only other
place it exists — do not delete it before the push). Then run 2: pin -> #v0.3.0,
wipe pkgcache + store entry + nimble.paths, `make nimble-deps`, re-verify.

2026-07-09  ISSUE 0015 addendum — nimExe() context fix (orchestrator spike folded in)
  status.nims used getCurrentCompilerExe() to run `nim e` and to set QT_PC_NIM.
  Correct under `nim <task> status.nims`; WRONG under `nimble <task>` (the
  manifest include's the driver), where it names nimble's EVALUATOR, not the
  pinned compiler. Replaced by nimExe(): $STATUS_NIM, else findExe("nim") when
  defined(NimbleVersion) — nimble marks its VM with --define:NimbleVersion and
  injects <store>/pkgs2/nim-<pinned>/bin onto PATH for tasks+hooks — else
  getCurrentCompilerExe(). selfExe()/querySetting(libPath) are never used.
  Verified: throwaway package showed defined(NimbleVersion) true only in the
  nimble VM, with findExe("nim") -> the store nim; in-tree, `nim app` writes
  tool key "… 2.2.10" and `nimble buildArtifacts` writes "… 2.2.4" (73.0 s, rc 0),
  `nimble build` 108.1 s rc 0, STATUS_NIM=/nonexistent/nim fails loudly.
  Compiler provenance recorded per leg in the 0015 verification record: all
  `nim app`/`nim e`/prl-to-pc `nimble test` legs ran on NBS nim 2.2.10, all
  `nimble build`/`nimble buildArtifacts` legs on the pinned store nim 2.2.4.
  No cross-path parity is claimed; store byte-identity is unaffected. Cost of
  the divergence today: alternating front doors rebuilds prl-to-pc's two tools
  (~2 s), because NimVersion is in their content key. Goes away with 0018.
  Both walls added to .phase2-vendor-backup/status-go/AGENTS.md.

--- 2026-07-09 — ORCHESTRATOR RE-VERIFICATION of 0015 (independent) ---
Every claim in the 0015 agent's verification record was re-checked by the
orchestrator, not taken on trust. All held.

  - env, GENERATED kit (6.11.0): QT_PC_MODE=generated, reason "Qt6Core.pc
    libdir mismatch (broken prefix)", PKG_CONFIG_PATH -> the store's committed
    .pc tree, prefix override + wrapper prepend present.
  - env, SYSTEM kit (6.11.1): QT_PC_MODE=system, reason "libdir matches qmake
    QT_INSTALL_LIBS", PKG_CONFIG_PATH -> the KIT's own pkgconfig dir; no
    wrapper, no prepend, no prefix override. The agent's amendment to the
    criterion is CORRECT: "empty env" would have silently selected brew's
    ambient Qt. Criterion text was wrong; the code is right.
  - generate from the store copy: refuses with an actionable message.
  - Store entry byte-identical across BOTH a `nim app` build and a
    `nimble build`: 1161 files, aggregate md5 6644afbd… unchanged. `.pcwrap`
    absent from the store; tools live in .prl-to-pc-build/.pcwrap.
  - Env cache carries the REPO-LOCAL prepend (not the store default that a
    bare `env` invocation prints); config.nims replays it idempotently.
  - no-op `nim app`: 7.10 s / 6.93 s steady state (first run 10.3 s was cold
    cache) — the agent's 6.78-7.25 s range is honest.
  - `nimble build` rc=0 in 1:44; binary 30262032 B, 13 Qt libs linked.
  - Upstream: qt-pkgconfig.mk has ZERO diff vs origin/main (dual interface
    intact); +317 lines qt_pkgconfig.nims, +287 lines tests.
  - nimExe() now branches on VM context, citing the 2026-07-09 measurement.

COMPILER NOTE (unresolved until 0018): the driver ran under NBS nim 2.2.10
while `nimble build` used the pinned store nim 2.2.4. Both produce a working
binary; no cross-path binary parity is claimed for 0015.

PUSH GATE OPEN: prl-to-pc commit 4a31fc06e8c8e38fca6a9396d7cedffacecda010 is
NOT on the remote (verified via git ls-remote). It exists in vendor/prl-to-pc
(gitignored) and, as a safety net, on backup/nimble-0015-v0.3.0 in
.phase2-vendor-backup/prl-to-pc. The app pin names that commit as an interim;
the pin becomes #v0.3.0 after the human pushes.

================================================================================
2026-07-09  ISSUE 0015 — DONE (run 2: push gate closed, tag pin re-solved)
================================================================================
The human pushed prl-to-pc main (4a31fc06) and the annotated tag v0.3.0
(c758a2ab, peels to 4a31fc06). Confirmed via git ls-remote.

App pin bumped: #4a31fc06… -> #v0.3.0.

Remote re-solve, proved from scratch: all three
~/.nimble/pkgcache/githubcom_statusimprltopcgit_* clones (incl. the pre-push
seed), the prl_to_pc store entry, nimble.paths and .prl-to-pc-build moved
(NOT rm'd) to ~/.nimble/bak-0015-postpush/. Then `make nimble-deps` = 69.9 s,
rc 0, log carries "Downloading https://github.com/status-im/prl-to-pc.git using
git". Fresh pkgcache clone minted for the ref (…_v030 — the key embeds the ref,
so the pkgcache-staleness wall doesn't apply). Store entry:
prl_to_pc-0.3.0-efc5e3d18f1e6e11e2f3a6b6ed034e84d634771b, nimblemeta url =
github, vcsRevision 4a31fc06…, specialVersions ['0.3.0', '#v0.3.0'].

NEW FINDING (walls doc): the store entry checksum is IDENTICAL to the interim
SHA-pinned one, and a per-file md5 diff shows why — of 1161 files, 1160 are
byte-identical and the ONLY difference is nimblemeta.json (specialVersions
'#4a31fc06…' -> '#v0.3.0'). nimble's content checksum covers the package tree
and excludes the metadata file nimble writes itself. Same mechanism as 0014's
loopback-vs-github collision. A pin-form change therefore never re-materializes
the tree.

Re-verified against the remote-resolved store copy (Qt 6.11.0, Generated mode):
  nim vendors -> pin …/prl-to-pc.git#v0.3.0
  full nim app 62.9 s rc 0; launch smoke OK (libstatus/libsds/libStatusQ,
    QtCore from ~/Qt/6.11.0/macos, clean SIGTERM)
  store byte-identity across the build: 1161 files identical (and still
    identical after nimble build + the develop round-trip)
  no-op nim app 7.92 / 7.39 / 7.34 s;  nimble build 128.9 s rc 0
  nim qtPkgconfigGenerate -> exit 1, refuses the store copy
  develop round-trip on the TAG pin: develop reused the checkout ("HEAD 4a31fc0;
    pin v0.3.0"), marker edit observed (73.3 s), cache key re-rooted to the
    checkout; after revert, `undevelop` SUCCEEDED WITHOUT --force (the commits
    are pushed now — the unpushed-work guard correctly stands down), and the
    next build (74.6 s) dropped the marker and re-keyed to the store entry.

All 11 acceptance criteria met. Two are recorded as amended/scoped rather than
literal (System-mode `env` is not empty — it must still export PKG_CONFIG_PATH
or pkg-config picks brew's ambient Qt; `make qt-pkgconfig*` "no longer exists"
scopes to the APP's targets, the mk's survive with the include the mobile +
nim-test-run legs still need). Orchestrator independently re-verified run 1.

Cleanup available: ~/.nimble/bak-0015-postpush/ (pre-push clones + old entry)
can be deleted; the .phase2-vendor-backup/prl-to-pc backup/nimble-0015-v0.3.0
branch is now redundant with the remote.

--- 2026-07-09 — ORCHESTRATOR RE-VERIFICATION of 0015 run 2 ---
Push verified independently BEFORE resuming the agent: refs/heads/main =
4a31fc06…, refs/tags/v0.3.0 = c758a2ab… (a real ANNOTATED tag object) whose
peeled target v0.3.0^{} = 4a31fc06…. The pin resolves the peeled commit.

Re-verified after run 2:
  - pin = https://github.com/status-im/prl-to-pc.git#v0.3.0
  - the wipe was REAL: old SHA-keyed pkgcache moved to ~/.nimble/bak-0015-
    postpush, new key githubcom_statusimprltopcgit_v030 cloned at 14:57.
  - nimblemeta: url = the GitHub remote, vcsRevision = 4a31fc06…,
    specialVersions = ['0.3.0', '#v0.3.0'].
  - PREDICTION HELD: the tag-solve produced the SAME store checksum as the
    interim SHA-solve (prl_to_pc-0.3.0-efc5e3d1…) — content-addressed,
    pin-shape-independent.
  - `nim app` from the tag pin: rc=0, 7.9 s; System-mode kit still probes
    system. Store unchanged across the build.

NEW GOTCHA (cost the orchestrator a false alarm — record it):
  A whole-directory fingerprint of a store entry is NOT stable across a pin
  SHAPE change, even when the entry name is identical. Payload is byte-
  identical (1160 files, md5 2dafd66f…), but `nimblemeta.json` differs:
  specialVersions '#4a31fc06…' -> '#v0.3.0', and the tag-solve additionally
  lists "/nimblemeta.json" inside its own `files` array. Byte-identity checks
  on store entries MUST exclude nimblemeta.json; otherwise a pin bump looks
  like content drift.

=== 2026-07-10 — Issue 0016: buildArtifacts goes native — DONE (pass: true) ===

Picked up: docs/superpowers/issues/0016-build-artifacts-native.md
(PRD row 0016; brief's settled decisions: stale() is DRIVER-PRIVATE, the
issue's "What to build" + the PRD's Implementation Decisions are approved)

Commits: f19b0479d3, a7017dbfd5, 0f913789c9 (+ this doc slice)

What was built:
- status_artifacts.nims (new include, driver-side ONLY — never config.nims,
  which runs on every nim invocation incl. nimsuggest and must never gate on
  the filesystem). One proc per artifact; buildArtifacts is an ordered call
  list; the nimble before-build hook contract is unchanged.
- stale(outputs, inputs): make semantics, ONE subprocess per call — inputs
  stream through a heredoc into the shell's own `-nt` (no exec per file, no
  ARG_MAX). Prefactor for 0017's client compile.
- Exactly two gating patterns, both documented in the driver header: stale()
  and the key file. Audit table in the issue's verification record.
- ONE generic cmake proc for StatusQ / DOtherSide / status-keycard-qt /
  translations. `cmake --build` always runs (its check-build-system rule IS
  cmake re-configuring itself); the CONFIGURE is key-file gated on its
  argument list — the one input cmake cannot see. The 0011
  FETCHCONTENT_SOURCE_DIR_<NAME> pair contract survives verbatim and a
  develop flip now changes the KEY instead of relying on the artifact being
  deleted (strictly safer than the make rule it replaces).
- qrcodegen: {.compile.} in the Nim wrapper. Static-lib target, --passL (x3
  sites), `make clean` recursion and the platform-sentinel entry all deleted;
  ADR 0003 amended.
- Bootstrap: targeted submodule init (DOtherSide, QR-Code-generator,
  SortFilterProxyModel) + macOS brew bottles.
- Setup stamp RELOCATED: nimble setup gated on nimble.paths vs
  lock/manifests/overlay; applyOverlayNow() runs inside the same gate (the
  ordering wall from 0015 is preserved by construction).
- compileTranslations / updateTranslations are driver tasks. REBUILD_NIM →
  developModeForce(). `run` assembles+launches natively. client-deps deleted.

Empirics that shaped it:
- StatusQ's cmake CONFIGURE rewrites ui/StatusQ/build/Qt<ver>/
  TestConfig.generated.qrc EVERY run. make's UI_SOURCES glob covers ui/**,
  so resources.rcc was permanently stale. Fix: scan only what
  ui/generate-rcc.go itself walks (it prunes StatusQ/vendor/tests/
  node_modules). Also added *.qm to the pattern, since translations left the
  artifact set and nothing else would notice a compileTranslations run.
- Running the three cmake configures unconditionally (the issue's literal
  wording) costs 5.26+0.57+1.13 s → no-op 14.1–17.7 s, blowing criterion 2.
  Key-file gating the configure gets it to 4.6–5.6 s (baseline was 8.1 s).

NEW WALL (the honest one): **"no make process in the tree" is not achievable
and its stated test does not prove it.** cmake's default generator on
macOS/Linux is Unix Makefiles → `cmake --build` execs CMAKE_MAKE_PROGRAM, an
ABSOLUTE /usr/bin/make baked into CMakeCache at configure time. 31 make
processes sampled under a green `nim app`. Removing make from PATH proves
nothing: cmake finds /usr/bin/make outside PATH at configure time too
(verified on a throwaway project). The second source is status-go's own
Makefile: `statusgo-shared-library` exists ONLY there — statusgo.nims exposes
libstatus (STATIC) + libsds*, not the shared flavor. What IS true and
verified: no target of THIS repo's Makefile is invoked by the host dev flow,
and both front doors are green with make stripped from PATH. Recorded in the
walls doc; -G Ninja is the fix if the literal claim is wanted.

Verification (all 13 criteria; record in the issue): make-off-PATH build
rc=0; no-op 4.61–5.57 s over 10 runs; wiped store (NIMBLE_DIR scratch, 0
pkgs2 entries) + all artifacts dropped → 9:04.14, 46 entries materialized,
bare-exec launch OK; nm shows qrcodegen_* T-symbols and 0 libqrcodegen on the
link line; rcc skipped/regenerated correctly (incl. after compileTranslations);
submodule deinit → Bootstrapping → rc=0 1:14.9; setup skipped on unchanged
inputs, re-run on a touched manifest and on an overlay flip; compileTranslations
is a task and does not run in a build; develop round trip proven for BOTH
flavors (status-keycard-qt: key flip + checkout edit compiled + undevelop;
prl-to-pc: re-solve, 1 overlay entry, env cache re-keyed, undevelop back to
pkgs2/prl_to_pc-0.3.0-efc5e3d1…); sentinel desktop→iOS (3:45, Status.app,
0 duplicate symbols)→desktop (1:32, artifacts rebuilt, launch OK);
`make client-deps` → "No rule to make target"; nimble build rc=0 2:17.

Gotchas for next sessions:
- {.compile.} + a `-lqrcodegen` on the same link is safe: the linker never
  pulls an archive member whose symbols are already defined (iOS leg proved
  it). Mobile keeps its own libqrcodegen.a for now.
- The client is NOT relinked when a vendor artifact changes (order-only
  semantics, same as make). A libstatus rebuild leaves the old binary; it
  still loads the new dylib via @rpath.
- stale() compares SECONDS (shell `-nt`), make compares nanoseconds.
- On Windows stale() is always true → every gated artifact rebuilds. Ported,
  unverified. 0017 will want the walls-doc content-key variant anyway.

Carry-overs / follow-ups (recorded in the issue, NOT implemented):
1. Fresh clones lose ui/i18n/*.qm → resources.rcc has no catalogs until
   `nim compileTranslations status.nims` runs. Direct consequence of the
   approved "translations are not part of the client's artifact set". Decide:
   commit the .qm, run the task in bootstrap(), or accept.
2. -G Ninja would make criterion 1 literally true.
3. statusgo-shared-library wants a nimscript task (fold into 0018's
   status-go-deps move).
4. stale() Windows content-key variant.
5. run-* make targets are now dead (0017 deletes them).
6. .qmake_previous superseded by .status-client.key (dies with 0017).

Standing items unchanged: NBS deletion (0018), CI migration (0019), seaqt
qt-6.11 evaluation, 0006 (upstream-gated), status-go nimble-phase1-pin →
develop PR, 0013 bootstrap-ordering follow-up.

--- 2026-07-10 — 0016 review round (two-axis) + user adjudications + fix wave ---

Orchestrator independently re-verified the headline claims (client-deps gone,
qrcodegen in-binary, stale() driver-private, no-op 4.71–5.03 s) — all held.
Four spec conflicts went to the human; all four ACCEPTED as built:

1. Criterion 1 accepted AS MET per its own verification method. cmake's
   Unix-Makefiles generator and cold-libstatus's foreign make are accepted
   residuals; -G Ninja and a status-go shared-library nimscript task remain
   recorded follow-ups.
2. The key-file-gated cmake CONFIGURE is accepted over the spec's "invoked
   unconditionally" wording. The issue's "What to build" bullet was amended in
   place with the reason: three unconditional configures = 14–17 s no-op vs a
   ~7 s criterion; `cmake --build` re-configures itself on CMakeLists changes;
   the key covers the one input cmake cannot see (its argument list) and makes
   the develop redirect flip safe BY CONSTRUCTION rather than by relying on
   the artifact having been deleted.
3. buildClient()/launchHostApp() pulled forward from 0017 — accepted;
   criterion 1 forced it. 0017's remaining scope: nim tests, Windows launcher,
   run-*/nim_status_client target deletion + .qmake_previous, packaging against
   the driver binary, the explicit force flag, Windows flag branches, portable
   stale().
4. The Windows always-stale behaviour is a DISCLOSED REGRESSION, not an
   unverified leg: slow-but-correct replacing fast-but-wrong (the old
   nimblePathsStale() returned false on Windows — it declared the resolution
   fresh without looking, and leaned on make's stamp rule, which this issue
   deletes). 0017's content-key stale() fixes it.

Fix wave (c072056813, ed3208e2b9):
A. fetchBottles()'s dirExists is classified as BOOTSTRAP, in the header and in
   the audit table — bootstrap materializes inputs a fresh clone lacks that
   nothing in the build can invalidate. NEW follow-up: the bottle FLAVOR is
   unkeyed (cross-desktop reuses arm64_sonoma). Pre-existing — make's
   $(BOTTLES) had the same hole — and not fixed here because keying it forces
   a one-time network refetch on every existing tree and CI agent.
B. keyStale(keyFile, key, witness) is now the ONE key-file spelling. The
   cmake configure's bare fileExists(CMakeCache.txt) is folded into it. There
   is no bare fileExists gate left in the file. Verified: rm CMakeCache while
   the key file survives → reconfigures.
C. prepareStatusgoScratch's open-coded origin compare → keyStale(originFile,
   storeRoot, witness = scratch/statusgo.nims). Verified: corrupt the origin →
   "prepareStatusgo: refreshing".
D. uiSources' prune is now `*/<name>/*` per name. NOTE — the reported bug was
   a FALSE POSITIVE: find's -path uses fnmatch WITHOUT FNM_PATHNAME, so
   `ui/*/vendor/*` already matched ui/a/b/vendor/x.qml (proved). Kept the new
   spelling because it says what it means. Re-verified with a synthetic
   depth-3 pruned file.
E. clientKey() under-keyed the relink — REAL bug. config.nims' envOr(NAME,
   derived) prefers an exported value, so QT_LIBDIR / STATUSGO_LIBDIR /
   NIMSDS_LIBDIR / STATUSQ_INSTALL_PATH / STATUSKEYCARD_QT_LIBDIR /
   DOTHERSIDE_LIBDIR / MACOSX_DEPLOYMENT_TARGET each move link flags and baked
   rpaths while touching no file stale() watches. All now in a named
   clientFlagEnv list; prl-to-pc's cached env joined clientSources() (it
   decides what `pkg-config --libs Qt6…` puts on the link line). Verified:
   export STATUSGO_LIBDIR → relink; unset → relink; then stable.
Minor applied: developModeForce() → applyDevelopModeArms() (it mutates the
tree; a query name invites a second call); qmlDebug() shared; the two identical
mobile arms of `app` folded; rccSkipDirs' comment names ui/generate-rcc.go as
the source of truth.

Post-fix: no-op 4.37/4.57/4.45/4.61/4.71 s; `nim app` rc=0; `nimble build`
rc=0. Envelope holds (it improved).

--- 2026-07-10 — issue 0017: the driver owns every Nim compile ---

Landed: the client, the Nim test suite and the Windows launcher are driver
tasks; the root Makefile invokes no `nim c` / `nim e` / $(ENV_SCRIPT) and
carries no NIM_PARAMS. Packaging (pkg-macos/linux/windows, flatpak, AppImage)
asks the driver for the binary it ships. `run`/`run-macos`/`run-linux`/
`run-linux-gdb`/`run-windows`, `nim_status_client`, `nim_windows_launcher`,
`nim-test-run/%`, `tests-nim-linux`, `.qmake_previous`, `.update.timestamp`,
`NIM_SOURCES` and `REBUILD_NIM` are deleted. `--force` on app/run replaces
REBUILD_NIM for humans; a developed clientRebuild Vendor forces the compile by
itself. Windows client flags moved into config.nims (ported, unverified).

ORDERING THAT MATTERED: the parity comparison ran BEFORE deleting
`make nim_status_client`. Result is better than the 0013 floor: **0 differing
bytes**, same LC_UUID, same `nm -a` (OSO stabs included). Both front doors now
use nim 2.2.4, so the 0015-era compiler divergence is gone. FIRST attempt
compared a driver binary left from the previous commit and __TEXT,__const
differed — it decoded to the DESKTOP_VERSION/GIT_COMMIT strdefines. Neither
make's NIM_SOURCES nor the driver's source key watches git HEAD (follow-up).

`stale()` is now a portable CONTENT key (user adjudication). The walls-doc
spelling — hash(readFile) per input in the nimscript VM — costs 13-16 s for
this repo's 1762 inputs / 11.7 MB (VM hashes at ~1.1 us/byte). Instead: ONE
`find -print0 | xargs -0 cksum | sort | cksum` per gate. 0.2 s (src) / 1.1 s
(ui), same subprocess count as the mtime scan. A/B on the same (loaded) tree,
`nim buildArtifacts` x5: NEW cpu 13.00-14.08 s vs OLD cpu 13.54-14.23 s — no
regression. Wall times are meaningless right now: SIX ORPHANED qmlprofiler
PROCESSES (ppid 1, 22h-2d old, ~65% CPU each) + 201 WebKit.WebContent had the
machine at load 109. 0016's 4.4-5.6 s no-op is not reproducible today under
EITHER engine (it measures 13-22 s wall for both). Re-measure when idle.

Traps the content key introduced (both handled, both in the walls doc):
- `forceTouch` FORCE arms silently die. nim-sds' arm touched a derived
  nimble.paths; buildLibsds() now forces on `"sds" in readOverlay()`.
- nimble.paths is no longer the setup stamp (`.status-setup.key` is), which
  CLOSES the walls-doc applyOverlay trap: a hand-run `nimble setup` can no
  longer make the next build skip resolution.
New gitignored key files: .status-setup.key, .status-rcc.key, .libsds.key.

NEW WALL: `make nim-test-run/<test>` NEVER worked on macOS. The recipe exports
LD_LIBRARY_PATH (ignored by dyld) and config.nims' non-client arm bakes rpaths
only for env vars that are set — NIMSDS_LIBDIR is not one, so the test binary
died with `Library not loaded: @rpath/libsds.dylib`. Reproduced before deleting
the target. The driver's `tests` task compiles and runs as two steps and hands
the run the same library path launchHostApp gives the app. 8 suites / 59 [OK] /
0 failed, 15:26.

Verification (evidence in the issue): check-no-nim-compiles.sh green + negative
control; parity 0 bytes; `nim tests` green; `nim windowsLauncher --compileOnly`
emits Windows C (full link needs mingw -> ported, unverified); every deleted
target reports "No rule to make target"; run-storybook*/run-statusq-* survive
(other products, no nim, used by Jenkinsfile.tests-ui); --force rebuilds,
touch-without-edit does not; rcc key flips on content not mtime; develop round
trip with nimqml (clientRebuild) + sds: forced client every build, the checkout
edit reached the compile (static echo probe), undevelop returned to the pins;
`nim app` and `nimble build` both rc=0.

NOT VERIFIED: `make pkg-macos` signed dmg — this machine has no `nix` (which
supplies dmgbuild) and no MACOS_CODESIGN_IDENT. What IS proven: the dmg target's
only client-producing step is `nim app status.nims`, RESOURCES_LAYOUT=-d:production
relinks by key, and the .app's executable has the same __TEXT,__text md5 as
bin/nim_status_client (macdeployqt only strips __LINKEDIT: 15.9 MB -> 2.5 MB).
Windows and Linux legs: ported/re-pointed, unverified. Jenkinsfile.tests-nim and
Jenkinsfile.flatpak were edited because 0017 deleted the targets they invoked
(CI itself is 0019).

Carry-overs (recorded in the issue, NOT implemented):
1. client is not rebuilt when git HEAD moves (DESKTOP_VERSION/GIT_COMMIT).
2. prepareQtPkgconfig() runs prl-to-pc's `tools` (a nim e, 1.95 s) every build.
3. no-op is now dominated by cmake --build no-ops (StatusQ 4.3 s, automoc).
4. REBUILD_UI has no driver equivalent.
5. first build after this lands re-runs setup/rcc/libsds once (new key files).
6. `make all` is the last root-Makefile target that builds the app.
7. `nim run status.nims` forwards no app ARGS (make run ARGS= did).
8. `nim tests` costs 15 min (per-test nimcache + -d:lto).

================================================================================
2026-07-10 — issue 0017 REVIEW FIX WAVE (fresh implementer; in-tree build lock)
================================================================================

Two-axis review of 994c7913e5..c942984e66 returned 3 Critical + 5 Important +
10 Minor. All applied. Six commits, 3e26014e80..7d9f79559d.

CRITICAL
C1 contentKey() died under dash. `set -o pipefail` is a POSIX *special builtin*,
   so an unknown option to it is FATAL in a non-interactive shell — and
   `2>/dev/null` hid the message. /bin/sh is dash on Debian/Ubuntu = the flatpak
   CI image. rc=2, no digest, every gate (nimbleSetupIfStale, buildResources,
   buildLibsds, buildClient) dead. Fix: probe in a subshell —
   `(set -o pipefail) 2>/dev/null && set -o pipefail;`. Verified on /bin/dash,
   /bin/sh and /bin/bash (same digest; bash still gets real pipefail).
C2 The empty-input digest was well-formed: `find <nothing> | xargs -0 cksum |
   sort | cksum` = "4294967295 0", rc=0. An artifact whose inputs all vanished
   recorded that key and read FRESH forever. contentKey now fails loudly on a
   `<crc> 0` digest and on an all-missing `extra` set. This is ALSO the guard
   that saves shells lacking pipefail: there a broken `find` exits 0 with
   exactly that digest.
C3 Packaging targets had zero always-stale prerequisites since the .PHONY
   nim_status_client target died: with pkg/Status.dmg present, `make pkg-macos`
   was "up to date" and never dispatched the driver. Proven against HEAD's
   Makefile. Fix: a `FORCE` phony prereq on all four packaging artifacts,
   defined AFTER `all:` so it cannot steal the default goal.

IMPORTANT
I1 Windows `-d:lto` was unconditional; make gated it on INCLUDE_DEBUG_SYMBOLS.
I2 launchHostApp had no Windows arm — `run-windows`'s DLL staging (no rpath on
   Windows) was deleted with the target. Ported, unverified.
I3 `.libsds.key` was written under statusgoBuildRoot() = the vendor/status-go
   CHECKOUT in develop mode → permanently dirty submodule. Moved to the repo
   root; added to .gitignore (follow-up 5 had claimed all three key files were
   ignored; only two were).
I4/I5 Shared-definition duplication: the Qt pkg-config env replay and the seaqt
   pkg-config link libs were byte-copies of config.nims; dotherSideLibDir() and
   keycardBuildDir() carried "keep in sync" comments and HAD ALREADY DIVERGED
   (config.nims appends cmake's per-config lib/<Release|Debug> leg on Windows).
   All hoisted into status_env.nims. The cmake BUILD dir and the LINK dir are
   different directories on Windows, so they became separate procs.

NEW WALL (not in the review; found while deleting the dead `forceTouch` field):
   A CONTENT KEY CANNOT BE FORCED BY TOUCHING A FILE — and 0017 left TWO such
   touches. The sds forceTouch arm was the known one. The other was in
   applyDevelopModeArms(): when a hand-run `nimble setup` regenerates
   nimble.paths without the overlay, it `touch`ed nimble.overlay to invalidate
   the setup stamp. Under the content key that is a no-op → the next build skips
   resolution, never applies the overlay, and silently compiles a DEVELOPED
   vendor against its PIN (ADR 0007's one forbidden failure mode). Fixed by
   rmFile'ing .status-setup.key. Generalization for the next agent: after
   swapping an mtime gate for a content key, grep the tree for `touch` and ask
   of each one "what content does this change?".

RETRACTED (do not re-propose): adding --skipParentCfg:on to the driver's `nim c`
   recipes. The client's project file is src/nim_status_client.nim, so the
   REPO-ROOT config.nims is itself reached by the parent-dir walk — the flag
   would strip the client's entire flag set. Recorded as issue follow-up 9.

MINOR: M1 stale() one-arg overload replaces the stale("",[out]) sentinel; M2
   buildArtifacts rejects --force (it compiles no client) and buildHostArtifacts
   lost its unused force param; M3 check-no-nim-compiles.sh reported line numbers
   of the STRIPPED stream (blank lines filtered), missed `nim compile`, and its
   comment-strip ate the char before '#'; M4 dead NIM_STATUS_CLIENT deleted;
   M5 forceTouch deleted; M6 stale docs (mobile/TROUBLESHOOTING.md `make run`,
   BUILDING.md's emptied troubleshooting section); M7 `tests` re-implemented
   buildHostArtifacts' prologue and omitted buildWindowsImportLibs (broken
   Windows test link) → extracted prepareHostBuild(); M8 buildWindowsLauncher
   takes the parsed --compileOnly value. M9/M10 recorded as follow-ups 10/11.

VERIFIED: check-no-nim-compiles.sh green (+ negative control with correct line
numbers); `nim app status.nims` rc=0 then two no-ops at 11.6 s / 11.7 s;
`nim tests status.nims utils_test` rc=0 (62 s, 9 OK, 0 FAILED);
`nim windowsLauncher status.nims --compileOnly` rc=0 (18 windows C sources);
`touch pkg/Status.dmg && make -n pkg-macos` -> `nim app status.nims`;
dash/sh/bash digest probes. NOT VERIFIED: everything Windows (I1, I2, contentKey
under msys2), Linux packaging, and the dmg signature — no hosts, unchanged.

=== 2026-07-12 — Issue 0018: NBS deleted (nimble is the only prerequisite) — DONE (pass: true) ===

The vendored nimbus-build-system is GONE — submodule, makefiles, vendored
compiler, env-script wrapper. The Nim-side machine prerequisite is NIMBLE, and
the compiler is whatever nim_status_client.nimble pins (nim == 2.2.4), which
nimble materialises in its store.

- Root Makefile: no external include; local V / HANDLE_OUTPUT / BUILD_MSG /
  .SILENT replace variables.mk+targets.mk. Deleted: BUILD_SYSTEM_DIR, both
  -includes, USE_SYSTEM_NIM (+ its PATH arm), the .DEFAULT submodule auto-init
  (the driver's bootstrap owns it since 0016), deps, update, status-go-deps,
  clean-common. `bottles` moved out of the macOS arm (it was UNDEFINED off
  macOS, so `deps`' bottles prerequisite could not even parse there).
  status-go-deps' one line (go install protoc-gen-go) is inlined in $(STATUSGO)
  — the driver's buildLibstatus() has run the same line since 0016. Moving it
  into status-go's nimscript needs a status-go commit+push+pin bump (forbidden
  here) — follow-up 4.
- mobile/scripts/buildNimStatusClient.sh: the LAST consumer of the NBS
  env-script wrapper. With USE_SYSTEM_NIM=1 (the only mode this repo ran) that
  wrapper only echoed "[using system Nim]" and exec'd the same nim → it becomes
  a plain `nim c`. What guarantees the PIN is PATH: nimble injects the store
  compiler for tasks/hooks; `source ./env.sh` does it for a bare make leg.
- env.sh: was `source vendor/nimbus-build-system/scripts/env.sh`; now wraps
  `nimble shellenv`.
- CI/fdroid/container: `make update` + `make deps` → `nimble setup` (forced by
  the deletions; 0019 owns CI). macOS Deps stage keeps its GITHUB creds block
  around `make bottles`.
- Submodule removal (P2 playbook): backup branch backup/nimble-0018-nbs in the
  module's own git dir; .gitmodules staged BEFORE `git rm --cached`; working
  tree moved to .phase2-vendor-backup/nimbus-build-system with core.worktree
  repointed. NOTHING under the shared .git/modules was pruned/deleted/rewritten
  (the parent checkout's other branches still use NBS); its
  .git/modules/vendor/nimbus-build-system verified intact afterwards. 5
  submodules left (DOtherSide, QR-Code-generator, SortFilterProxyModel,
  fcitx5-qt, mobile/vendors/openssl) — pins, not Vendors.

NEW WALL (added to .phase2-vendor-backup/status-go/AGENTS.md): **`nimble
shellenv` lists $NIMBLE_DIR/bin BEFORE the pinned compiler's
pkgs2/nim-<ver>-<checksum>/bin.** ~/.nimble/bin/nim is a symlink — normally the
pin, but choosenim / `nimble install nim@X` repoints it, and it then SHADOWS the
pin in a "bootstrapped" shell. The issue's "a bootstrapped shell cannot drift"
holds only by luck of that symlink. env.sh now re-hoists the pinned bin to the
front, and BUILDING.md/SHARED-i3/fdroid/container all teach `source ./env.sh`
instead of a bare eval. A compiler GUARD (config.nims comparing
getCurrentCompilerExe() with the resolved pin) is a PRD-level decision — NOT
taken; recorded as follow-up 1.

VERIFIED (macOS arm64, Qt 6.11.0 macos + ios kits):
- NIM-FREE MACHINE, FRESH CLONE (PATH = one dir holding only a `nimble`
  symlink; `command -v nim` → nothing): `nimble setup` rc=0 52.9 s ("Info: using
  .../pkgs2/nim-2.2.4-b4bb510b.../bin/nim for compilation"); `nimble build` rc=0
  5:55.8 → runnable 30.2 MB client, with the driver's bootstrap initialising the
  submodules; `ps` caught the compile process = .../pkgs2/nim-2.2.4-.../bin/nim c.
  Then `source ./env.sh` → nim = the store pin (2.2.4) → `nim app status.nims`
  rc=0 53.8 s. (The bottle fetch 401'd for want of GITHUB_USER/TOKEN — a
  pre-existing prerequisite; the bottle was copied in and the run redone.)
- BOTH FRONT DOORS USE THE PIN: driver's client compile prints the store nim's
  config chain; nimble names it explicitly; and the two binaries are
  section-identical (__text/__const/__cstring/__data + nm md5
  e17529d38a0c3803edafb487700378d6 — the same md5 0017 recorded), residue 15 279
  bytes = LC_UUID + signature + OSO stabs (the 0017 parity floor).
- MOBILE LEG (iOS device, from a nim-free shell + `source ./env.sh`):
  `make mobile-build V=3` rc=0 3:46.6 → mobile/bin/ios/qt6/Status.app; the client
  compile is a bare `nim c --app:staticlib -d:ios …` whose config hints name
  .../pkgs2/nim-2.2.4-b4bb510b.../config/nim.cfg. Sentinel flipped back
  (ios-arm64 → darwin-arm64), desktop rebuild rc=0 1:34.
- MAKE: `make -n all` → `nim app status.nims`; deps/update/status-go-deps/
  deps-common/update-common/clean-common all "No rule to make target";
  pkg-macos/status-go/rcc/statusq/storybook-build/clean/nimble-deps/
  compile-translations all parse; scripts/check-no-nim-compiles.sh green.
- NO-OP ENVELOPE: 3.42 / 3.96 / 4.10 s (brief's idle-machine envelope 3.51–3.89;
  another agent was building a Nim compiler on the box for part of the session).

NOT VERIFIED: the signed dmg (no nix/dmgbuild/identity — unchanged from 0017,
checkbox left unticked); the Android leg (no SDK/NDK — same script line as iOS);
CI (0019 owns it: nothing yet carries the bootstrapped PATH across Jenkins `sh`
steps — 0017's follow-up 10); Linux/Windows (no hosts); the shellenv-shadowing
CONSEQUENCE (proven from PATH order, not reproduced — that needs a second Nim
installed on this machine).

================================================================================
2026-07-12  issue 0018 — REVIEW FIX WAVE (3 adjudications, 3 critical, 3
            important, 3 minor). Local only, nothing pushed.

A1 (user, amends the PRD) A DRIVER-SIDE COMPILER GUARD IS ADDED. The PRD said no
version guard was needed because "a bootstrapped shell cannot drift"; 0018 §7
falsified the premise (shellenv lists $NIMBLE_DIR/bin, whose `nim` symlink
choosenim repoints, BEFORE the pinned pkgs2 entry). status_artifacts.nims now
carries pinnedNimEntry() + guardPinnedCompiler(), called from buildClient() after
the staleness gate and just before `nim c`: two file reads on the compiling path,
nothing on the no-op path, no subprocess. It compares the compiler that WILL run
(getCurrentCompilerExe() — measured: it already resolves the ~/.nimble/bin/nim
symlink to the store path, so no realpath fork) against pkgs2/nim-<version>-
<checksum>: version from the manifest's `requires "nim == X"`, checksum from
nimble.lock's packages.nim.checksums.sha1. NEW FACT: nimble.paths carries NO nim
entry (the compiler is not a --path:), so the LOCK is where the resolution writes
the entry down — and its sha1 IS the store directory's checksum. NEVER in
config.nims: nimsuggest evaluates that file with its own compiler and would
false-positive on every keystroke.
  Evidence — this machine happens to carry THREE store nims (2.2.10, and TWO
  2.2.4s with different checksums), so the guard was unit-proved against all of
  them: PASS only under nim-2.2.4-b4bb510b (the lock's entry); nim-2.2.10 and
  nim-2.2.4-a092a045 (right version, wrong entry — version-only matching would
  have passed it) both fail loudly, naming `source ./env.sh`; STATUS_NIM is a
  deliberate, announced override. Happy path: `nim app status.nims --force` rc=0
  (32.6 s). `nimble build` rc=0 (1:46.6) with the guard SILENT — it is not on
  nimble's client-compile path, and nimble injects the pin for its own hooks.

A2 status-go-deps inline ACCEPTED; the status-go-side nimscript move is filed in
docs/superpowers/upstream-asks.md as a push-bearing human task. It collapses the
three surviving copies of `go install …/protoc-gen-go@v1.34.1`: Makefile:639,
status_artifacts.nims:550 (buildLibstatus), fdroid/build-app.sh:31.

A3 `nimble setup && source ./env.sh` is the blessed bootstrap (not the bare
eval). Issue + PRD + BUILDING.md say so.

CRITICALS — env.sh was the bootstrap SPOF, and all three were real:
  C1 `grep -m1` returns rc=1 on no match and a command substitution propagates
     it: under a caller's `set -e` (fdroid/build-app.sh has `set -eou pipefail`;
     ContainerBuilds uses `bash -c 'set -e && …'`) sourcing DIED SILENTLY and the
     WARNING branch was unreachable. Reproduced against the old file (rc=1, no
     output). Fixed with `|| true` inside the substitution.
  C2 A SOURCED file inherits the CALLER's positional parameters — so
     `if [[ $# -gt 0 ]]; then exec "$@"; fi` HIJACKED any argful caller.
     Reproduced against the old file: the caller's argv was exec'd and its own
     code after `source` never ran. The exec arm is now gated on
     executed-not-sourced. WALL for the next reader: in zsh, BASH_SOURCE is unset
     and `$0` is the sourced file in BOTH cases — the discriminator is
     ZSH_EVAL_CONTEXT (`toplevel:file` sourced, `toplevel` executed).
  C3 The hoist took the FIRST pkgs2/nim-*/bin on shellenv's PATH and asserted
     NOTHING about its version — with two store nims the wrong one silently wins,
     the exact class the file exists to prevent. It now parses the pin from the
     manifest (never hardcoded) and compares it to the version embedded in the
     store entry's own directory name (nimble mints that name — no `nim -v`
     fork), failing loudly on mismatch.
  M1 idempotent hoist + PATH dedupe (shellenv re-prepends its dirs to whatever
     PATH it inherits) + every STATUS_* temp unset on every exit path.
  PROBE MATRIX (/tmp/0018-probes/run.sh) 14/14: bash and zsh × sourced/executed ×
  args/no-args × set -e; the fdroid and ContainerBuilds call shapes; an argful
  caller must survive; a two-nim PATH must fail loudly (stub shellenv naming
  nim-2.2.10 first → rc=1 in both shells) with a pin-first positive control.
  Probe wall: `set -eou pipefail` is BASH syntax — zsh rejects it ("no such
  option: u") and never reaches env.sh; the zsh cell must use
  `set -e; set -u; set -o pipefail`.

IMPORTANT:
  I1 CI's half-bootstrap is NOT fixed here (orchestrator decision: 0019 owns
     ci/). Instead 0018's record §3 is retitled "Both LOCAL front doors …" and
     criterion 3 is scoped, and 0019 gains a "Blocked-by-0018 handoff" section:
     six pipelines run `nimble setup` with no PATH bootstrap in their LATER `sh`
     steps (macos, linux, windows, flatpak, tests-nim, linux-nix), Jenkinsfile.ios
     and .android have NO bootstrap at all while still compiling the client
     through a now-plain `nim c`, plus criterion-3's CI leg. A1's guard makes that
     gap fail loudly instead of silently.
  I2 One source of truth for the key files: every repo-root key file is
     `.status-<artifact>.key`, so `make clean`'s `rm -f .status-*.key` is TOTAL
     and no hand-kept list can drift from the consts. `.libsds.key` → renamed
     `.status-libsds.key` (the one name that broke the pattern); Makefile,
     .gitignore, BUILDING.md and 0017's gating-audit row updated; the
     contradictory comment above `clean` removed; a legacy `rm -f .libsds.key`
     stays for trees built before this commit. Keys INSIDE a build tree
     (.status-cmake.key, .statusgo-artifact-key) are exempt — they die with the
     tree they gate.
  I3 `app --os:ios|--os:android --force` now REJECTS the flag (it used to warn
     and ignore, while buildArtifacts rejected it — one file, two contracts). The
     rejection runs BEFORE applyDevelopModeArms(), which mutates the tree.

MINOR: M2 recorded (commit 96d76723d4's title overstates — prl-to-pc's
qt-pkgconfig.mk include remains, and is check-no-nim-compiles.sh's known blind
spot; no amend on a shared branch). M3 reviewed and accepted (deletion-list names
survive in comments only — signposts, not live references).

REGRESSION (all from a `source ./env.sh` shell): check-no-nim-compiles.sh rc=0;
`nim app status.nims` ×3 no-op 3.76/3.20/3.20 s; `nim app status.nims --force`
rc=0; `nim tests status.nims utils_test` rc=0 (all OK); `nimble build` rc=0
1:46.6; `make -n clean` rc=0. NOT re-verified (unchanged, unverifiable here):
signed dmg, Android leg, CI, Linux/Windows.

================================================================================
2026-09-15  REBASE onto master — worktree-nimble-migration replayed on 872090c705
================================================================================

Base moved 7e38d3873a (2026-07-01) → 872090c705 (2026-09-15): 1002 master
commits under the 92-commit branch. Grill session settled the tree first
(12 decisions; recorded in the commit messages of the follow-up commits):
true replay (history kept) in a throwaway worktree, semantic collisions fixed
in explicit follow-up commits (never folded into history), re-pin to master's
revisions, nim 2.2.10, DOtherSide stripped, ADR 0004 → 0007, docs record.

REPLAY: `git rebase origin/master`, 14 conflict stops (checkpoint 2c2ca26f60;
0009 fa06e48280; 0010 e37ea1a273; 0011 1a13e054eb + 67b3f91566; 0012
862c277244; 0013 ed854150f7; 0014 52ed78e049; 0016 f19b0479d3 + a7017dbfd5 +
c3e6034678; 0017 994c7913e5 + 1b54cfaaa9 + 28f7e10b00; 0018 96d76723d4 +
4598f6b35d + cd31c00a6f + 8f7c9cbe38). Resolution rule at every stop: the
branch's structure, master's newer intent folded in (kcSuffix nimcache, the
macOS rpath loop, Linux -fPIC, the iOS -d:lto removal, master's condensed ADR
0003 + the 0016 amendment, master's Jenkinsfile.macos layout with `nimble
setup`/`make bottles`). Six submodule modify/delete conflicts → deleted (the
branch's pins). `ci/Jenkinsfile.tests-nim` and `makefiles/nim-tests.mk` are
deleted on master; their branch-side edits were carried into
`ci/Jenkinsfile.linux` and the driver's `tests` task instead.

SEMANTIC COLLISIONS (git-silent; each its own commit on top of the replay):
1. DOtherSide — master dropped it (d3b8917a04); the driver still built and
   linked it. Stripped: cmake artifact, link inputs, Windows staging,
   DOTHERSIDE_LIBDIR key entry, status_env helpers; MONITORING cmake flags →
   StatusQ (where master put the tool).
2. Makefile-era NIM_PARAMS knobs master kept adding after 0017 made them dead:
   QML_DEBUG (-d:qmldebug/-d:qmlDebugPort/-DQT_QML_DEBUG), MONITORING
   (-d:monitoring), USE_SIMULATED_KEYCARD (-d:useSimulatedKeycard + own
   nimcache + -DUSE_SIMULATED_KEYCARD=ON for status-keycard-qt) → config.nims
   + clientFlagEnv; Linux -Wl,-rpath-link,<Qt libdir> (872090c705); run-macos'
   Assets.car + monitoring copies → launchHostApp; makefiles/nim-tests.mk
   (StatusQ-linking suites, per-file QT_MODEL_SPY, per-test nimcache,
   benches) → `nim tests status.nims [--benches]`; fix-wallet-migrations →
   $(STATUSGO_ROOT).
3. status_go wrapper — master moved it in-tree (aca56a93bb) and grew it by 3
   procs; the package shipped a July copy. Package wins (0001's contract):
   refreshed on status-go `nimble-phase1-pin-2`, desktop's src/status_go*
   deleted.
4. Pins — every converted vendor moved on master: status-go (see below),
   nim-keycard-go de7eec7d, nim-seaqt 7d40abd7 (branch qt-6.8: master moved
   to the Qt 6.8 generation; NO tag, orphan-branch hazard recorded in the
   manifest and upstream-asks), nimqml fa084a8d, prl-to-pc 03a8a917 (main;
   tag ask), status-keycard-qt 8582bffc (FetchContent). nim == 2.2.10 (CI
   images / flatpak image standardised on system nim 2.2.10 — 143fecd95d,
   45d68d8641). CONTEXT.md gains "Pinned compiler" vs "system nim".

STATUS-GO (cross-repo): the nimble package lives only on `nimble-phase1-pin`
(5 commits, 228 behind develop; develop's tip 9f09f902 has no manifest, so
"re-pin to master's SHA" was impossible as stated). Rebased those 5 commits
onto develop in a scratch clone → `nimble-phase1-pin-2` @ 4c4cfc271 (one
stop: tools/generate-cbindings — develop had made emission reproducible on
its own, its version kept; Makefile keeps NIM_SDS_VERSION derived from the
manifest, develop's ldflags win). The wrapper refresh and the nim-sds pin
change ride on a sixth commit. **PUSH PENDING (human): `git push origin
nimble-phase1-pin-2` from the scratch clone** — the app manifest already pins
4c4cfc271, which resolves only after that push.

NIM-SDS: develop's go.mod requires the `release/v0.3` FFI ABI (v0.3.3), so the
statusgo.nimble pin moved from the fork PR #85 head (5c89d61f, the 6-patch
queue on a master-line 0.3.0). First attempt pinned upstream `#v0.3.3`
directly: nimble rejects it ("Skipping package statusgo due to invalid
dependency" — release/v0.3 has TWO root manifests, reliability.nimble +
sds.nimble, plus the wrapper outside srcDir and undeclared deps). Pinned
instead to fork branch `nimble-v0.3.3` (alexjba/nim-sds @ a771a894) =
v0.3.3 + one commit that makes it a nimble package (ABI untouched; details in
upstream-asks). **PUSH PENDING (human): `git push fork nimble-v0.3.3`** —
the status-go manifest pins that SHA.

DOCS: ADR 0004 (develop-mode overlay) → 0007 (master took 0004 for the typed
task-completion handoff); 0019 gains "Master moved under this issue" (the
tests-nim/tests-ui/ios pipelines are gone — folded into linux/macos; system
nim 2.2.10 images; the override-status-go-ref stage assumes a submodule);
0001 addendum; upstream-asks updated (nim-sds, status-go branch, seaqt tag,
prl-to-pc tag, keycard pin).

NOT CARRIED (deliberate): master's `LINK_PCRE=0` (an NBS variable; NBS is
gone); master's BUILDING.md `~/go/bin` step (master dropped protoc-gen-go
installs, 61189797fe); the branch's `-d:lto` on the iOS client (master
removed it for a miscompile, 2026-07 — master's note kept).

VERIFICATION: see the addendum below (Linux x86_64 host, Qt 6.11.0, go
1.24.7, cmake 4.2.3; nim/nimble were NOT installed on this machine — a
nim-2.2.10 tarball in the session scratchpad provided `nimble`).
